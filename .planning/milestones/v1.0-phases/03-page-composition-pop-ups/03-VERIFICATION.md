---
phase: 03-page-composition-pop-ups
verified: 2026-05-14T16:00:00Z
status: human_needed
score: 5/5 success-criteria verified (re-verification after gap closure)
re_verification:
  previous_status: gaps_found
  previous_score: 2/5
  gaps_closed:
    - "GAP-01 (BL-03 / SC4 / PAG-08) — robots.txt ships Allow + Sitemap on production deploy"
    - "GAP-02 (BL-01 / SC5 / PAG-07) — homepage canonical has trailing slash (https://studiobluemli.com/)"
    - "GAP-03 (BL-02 / PAG-07) — per-piece og:image base reads process.env / PUBLIC_-aliased env vars and is preview-aware"
    - "GAP-04 (WR-06 / SC3 / PAG-05) — About signature renders outline ♡ (U+2661), not filled ♥ (U+2665)"
  gaps_remaining: []
  regressions: []
deferred:
  - truth: "SC2 (cron portion) / PAG-04 — Daily 3 AM PT auto-rebuild via Cloudflare cron"
    addressed_in: "Future phase (post-Phase 3)"
    evidence: "REQUIREMENTS.md PAG-04 explicitly marked '(deferred from Phase 3): ... Phase 3 spike confirmed integrated approach incompatible with @astrojs/cloudflare@13.5. Fallback: separate cron-only Worker. To be implemented in a later phase.' ROADMAP SC2 softened: 'Daily auto-rebuild cron deferred ... until cron is added in a future phase the founder triggers a rebuild manually.' Build-time TZ-correct bucketing (the other half of SC2) IS verified."
human_verification:
  - test: "Share https://studiobluemli.com/ in iMessage, Slack, and IG DM after production deploy and verify the unfurl preview renders title, description, and og:image (og-default.png)."
    expected: "All three platforms render a card with `Studio Bluemli — hand-clustered beaded earrings`, the og_description, and the og-default.png. The URL pill matches `https://studiobluemli.com/` (with slash)."
    why_human: "Each unfurl platform (Apple, Slack, Meta) has its own cache + image-fetch behavior; programmatic HTML inspection cannot prove the rendered card."
  - test: "Share https://studiobluemli.com/gallery/cluster-coral/ in the same three platforms; verify the per-piece og:image (hero-800.webp) renders."
    expected: "Card shows the cluster-coral hero photo as the unfurl image, not the default coral-on-cream mark."
    why_human: "Per-piece og:image override on apex is now correctly preview-aware; verifying the rendered preview in real platforms (vs static HTML inspection) is the only proof that the og:image actually loads at the URL."
  - test: "On a phone at 320px viewport (or DevTools iPhone SE preset), confirm the hero eyebrow `Studio Bluemli · NOPA, San Francisco` does not overflow or push horizontal scroll (IN-02 from the original 03-REVIEW; refreshed REVIEW left it as a known warning)."
    expected: "Eyebrow fits within the section padding; no horizontal scroll bar at 320px."
    why_human: "CLAUDE.md targets Lighthouse mobile ≥ 90 and phone-first responsive; visual verification at the smallest target viewport is the only way to know."
  - test: "After production deploy, curl https://studiobluemli.com/robots.txt and https://studiobluemli.com/sitemap-index.xml — assert robots body contains literal `Allow: /` + `Sitemap: https://studiobluemli.com/sitemap-index.xml`, and sitemap-index references sitemap-0.xml with all 11 entries (5 top-level + 6 gallery slugs) on apex."
    expected: "200 OK on both; robots body has Allow + Sitemap (no Disallow); sitemap-index references sitemap-0.xml; sitemap-0.xml contains 11 <loc> entries all on apex."
    why_human: "Local build confirms the bytes; only a production deploy curl can prove the deploy pipeline emits them. The deploy script now sets PUBLIC_DEPLOY_ENV=production, but verifying that the env var actually propagates on the real Cloudflare Workers Builds run is a production-only signal."
---

# Phase 3: Page Composition & Pop-ups Verification Report (Re-Verification)

**Phase Goal:** All five pages render their real content on preview — landing (hero + OPTIONAL mini-callout for next pop-up, omitted on zero upcoming per D-03 + 3 featured pieces + footer), gallery (from Phase 2), popups (timezone-correct upcoming + past archive, auto-refreshing daily), about (written portrait + closing photo strip reusing gallery hero WebPs per D-14), say-hi (Instagram DM link + mailto fallback, no form per D-18) — with per-page SEO meta and a published sitemap.

**Verified:** 2026-05-14T16:00:00Z
**Status:** human_needed (all 5 success criteria automatically verified; 4 human-verifiable items remain for unfurl + production-deploy validation)
**Re-verification:** Yes — re-run after gap-closure wave (plans 03-06 + 03-07). All 4 prior gaps closed; no regressions.

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| #   | Truth (ROADMAP SC)                                                                                                                                                                                                                                                       | Status                  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| SC1 | Landing renders hero + OPTIONAL mini-callout (omitted on zero upcoming per D-03) + 3 featured pieces + footer, all from content collections                                                                                                                              | ✓ VERIFIED              | `dist/client/index.html`: hero rendered (clamp-sized "bright, beaded, one of a kind" + tagline + 2 CTAs); zero `<section class="popup-callout">` element (empty-state correctly omits the callout per D-03 — the only `popup-callout` token in the HTML is in scoped CSS declaration, not a rendered section); 3 featured pieces (`/gallery/cluster-blush/hero-400.webp`, `cluster-cobalt`, `cluster-coral`); footer with mark.svg + @studiobluemli + hi@ email. |
| SC2 | (build-time) A pop-up dated for "today" in Pacific time stays in Upcoming all day in SF (does not flip at UTC midnight); after end_date the next deploy moves it to Past. (cron auto-rebuild explicitly DEFERRED — see deferred section.)                                 | ✓ VERIFIED (build-time) | `src/lib/popups.ts:67` uses `Temporal.PlainDate.compare(popupEndDate(e), today) < 0` where `today = Temporal.Now.plainDateISO('America/Los_Angeles')`. Date-only Temporal comparison cannot flip on UTC midnight. `start_time`/`end_time` are never parsed as date components (Concern 2 fix preserved). `/popups` renders empty-state copy correctly in the current zero-popup state.                                                                          |
| SC3 | About renders first-person written portrait + hand-font headline + "made with love from NOPA ♡" signature (D-16) + closing photo strip of 1–3 gallery hero WebPs (D-14), no empty press placeholders                                                                     | ✓ VERIFIED              | 3-paragraph first-person portrait rendered in `dist/client/about/index.html`; hand-font headline "made by hand" present; signature renders **outline ♡ (U+2661)** with `font-family:var(--font-hand)` (Caveat) at coral-500 — GAP-04 closed; 3 photo strip cells (cluster-blush, cluster-cobalt, cluster-coral hero-800.webp); zero `press`/`featured in`/`as seen in` matches. Heart glyph grep: ♡=1, ♥=0.                                                      |
| SC4 | Sharing apex URLs produces correct unfurl previews (title, description, og:image) AND `https://studiobluemli.com/sitemap-index.xml` + `/robots.txt` return valid content with sitemap reference                                                                          | ✓ VERIFIED              | `dist/client/robots.txt` (production-flagged build) contains literal `User-agent: *\nAllow: /\nSitemap: https://studiobluemli.com/sitemap-index.xml\n` — GAP-01 closed. `dist/client/sitemap-index.xml` references `sitemap-0.xml`; `sitemap-0.xml` has 11 `<loc>` entries (5 top-level + 6 gallery slugs) all on apex. Every page emits og:title, og:description, og:image, og:url, twitter:card, twitter:image. Human verification routed for live unfurls.    |
| SC5 | Every page's `<link rel="canonical">` points to the apex `studiobluemli.com` (not www, not preview)                                                                                                                                                                       | ✓ VERIFIED              | All 6 prerendered pages emit apex canonicals: `/` → `https://studiobluemli.com/` (with trailing slash — GAP-02 closed), `/about` → `…/about`, `/gallery` → `…/gallery`, `/popups` → `…/popups`, `/say-hi` → `…/say-hi`, `/gallery/cluster-coral` → `…/gallery/cluster-coral`. Smoke test confirms canonical stays apex even when `PUBLIC_CF_WORKERS_URL=https://preview.example.workers.dev` is set at build time. No preview-hostname leak.                      |

**Score:** 5/5 ROADMAP success criteria verified (with the cron portion of SC2 explicitly DEFERRED per ROADMAP wording).

### Deferred Items

| #   | Item                                                                | Addressed In                  | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ------------------------------------------------------------------- | ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | SC2 cron portion / PAG-04 — daily 3 AM PT auto-rebuild              | Future phase (post-Phase 3)   | REQUIREMENTS.md PAG-04: `"(deferred from Phase 3) ... Phase 3 spike confirmed integrated approach incompatible with @astrojs/cloudflare@13.5 ... Fallback: separate cron-only Worker. To be implemented in a later phase."` ROADMAP SC2 explicitly softened: `"Daily auto-rebuild cron deferred ... until cron is added in a future phase the founder triggers a rebuild manually."` Plan `03-05-SUMMARY.md` documents the spike-FAIL root cause (adapter overwrites `wrangler.jsonc`). |

### Resolution of Prior Gaps (from previous VERIFICATION.md)

| Gap (prior)                                   | Prior Status | Plan          | New Status   | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| --------------------------------------------- | ------------ | ------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| GAP-01 (BL-03) — robots.txt Disallow on prod  | failed       | 03-06         | ✓ RESOLVED   | `package.json:12` deploy script now `PUBLIC_DEPLOY_ENV=production astro build && wrangler deploy`. Built `dist/client/robots.txt` reads `User-agent: *\nAllow: /\nSitemap: https://studiobluemli.com/sitemap-index.xml\n`. `npm run ci:seo-check` exits 0 with `OK: GAP-01 / BL-03 — robots.txt has Allow + Sitemap (no Disallow).` `deploy:preview` intentionally NOT prefixed — preview deploys keep `Disallow: /` for staying out of indexes. Commit `a11e118`.                          |
| GAP-02 (BL-01) — homepage canonical no slash  | partial      | 03-06         | ✓ RESOLVED   | `src/components/SEO.astro:41` changed from `pathname === '/' ? '' : pathname` to `pathname === '/' ? '/' : pathname`. Built `dist/client/index.html` contains `<link rel="canonical" href="https://studiobluemli.com/">` (with trailing slash) and `<meta property="og:url" content="https://studiobluemli.com/">`. Sitemap entry `<loc>https://studiobluemli.com/</loc>` now matches canonical. Smoke test asserts the exact byte sequence. Commit `5c67814`.                            |
| GAP-03 (BL-02) — og:image base not preview-aware | failed     | 03-06         | ✓ RESOLVED   | `src/lib/site-url.ts:67-83` `resolveAssetBase()` now reads from both `process.env.CF_PAGES_URL/CF_WORKERS_URL/PUBLIC_SITE_URL` AND `import.meta.env.PUBLIC_CF_PAGES_URL/PUBLIC_CF_WORKERS_URL/PUBLIC_SITE_URL`. Smoke test second build with `PUBLIC_CF_WORKERS_URL=https://preview.example.workers.dev` produces `<meta property="og:image" content="https://preview.example.workers.dev/gallery/cluster-coral/hero-800.webp">` while canonical stays on apex. Commits `5c67814` + `a11e118`. |
| GAP-04 (WR-06) — About signature filled heart | partial      | 03-07         | ✓ RESOLVED   | `src/components/design-skill/About.jsx:50` now reads `<Mark.Heart color="var(--coral-500)" filled={false} />` (was `<Mark.Heart color="var(--coral-500)" />` — Mark.Heart's default is `filled=true`). Built `dist/client/about/index.html` contains exactly one `♡` (U+2661 OUTLINE WHITE HEART) inside the signature `<span style="color:var(--coral-500);font-size:16px;line-height:1">♡</span>` and zero `♥` (U+2665). Commit `9079326`.                                                |

### Required Artifacts

| Artifact                                        | Expected                                                                                                                | Status     | Details                                                                                                                                                                                                                          |
| ----------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib/site-url.ts`                           | Three helpers — `resolveCanonicalBase`, `resolveAssetBase` (reads process.env + PUBLIC_ aliases), `isProduction`        | ✓ VERIFIED | Imported by SEO.astro + robots.txt.ts; all three exports present; precedence chain documented in docstring; `process.env` + `import.meta.env.PUBLIC_*` dual-read pattern restored. (See WR-01 in 03-REVIEW for empty-string edge case — non-blocking warning.) |
| `src/lib/popups.ts`                             | TZ-aware `splitPopups()` with date-only Temporal compare                                                                | ✓ VERIFIED | Imports `Temporal` from `temporal-polyfill`; `popupEndDate()` uses `Temporal.PlainDate.from`; comparison is date-only in `America/Los_Angeles`; no Temporal type leaks across function boundary.                                  |
| `src/components/SEO.astro`                      | Emits canonical (with trailing slash on root) + og/twitter meta; no `<title>`                                           | ✓ VERIFIED | 9-element head emission per page; line 41 now `pathname === '/' ? '/' : pathname`; resolveCanonicalBase still pinned to apex.                                                                                                    |
| `src/components/PopupCallout.astro`             | Landing mini-callout                                                                                                    | ✓ VERIFIED | Used by `index.astro` conditionally on `hasUpcoming`; omitted in current zero-popup state.                                                                                                                                       |
| `src/pages/index.astro`                         | Hero + conditional callout + 3 featured + footer                                                                        | ✓ VERIFIED | Built HTML shows hero, 3 featured gallery thumbs, footer; mini-callout omitted on zero popups (D-03 ✓).                                                                                                                          |
| `src/pages/popups.astro`                        | Soonest + ALSO COMING UP + PAST + empty state                                                                           | ✓ VERIFIED | Empty-state copy `No pop-ups on the calendar yet —` rendered.                                                                                                                                                                    |
| `src/pages/about.astro`                         | Portrait + signature (outline ♡) + photo strip, no press placeholders                                                   | ✓ VERIFIED | All three sections render; signature shows outline ♡ (GAP-04 closed); zero press/featured-in matches.                                                                                                                            |
| `src/pages/say-hi.astro`                        | IG DM link + mailto, no form                                                                                            | ✓ VERIFIED | 0 `<form>` and 0 `<input>` matches; `https://ig.me/m/studiobluemli` + `mailto:hi@studiobluemli.com` both present.                                                                                                                |
| `src/pages/robots.txt.ts`                       | Production: Allow + Sitemap; preview: Disallow                                                                          | ✓ VERIFIED | Built output is `User-agent: *\nAllow: /\nSitemap: https://studiobluemli.com/sitemap-index.xml\n` under production-flagged build (GAP-01 closed). `deploy:preview` still emits Disallow (intentional).                            |
| `public/og-default.png`                         | 1200x630 fallback og:image                                                                                              | ✓ VERIFIED | Present in dist/client/og-default.png; referenced by every page's og:image and twitter:image in built HTML.                                                                                                                      |
| `dist/client/sitemap-index.xml`                 | Valid sitemap-index referencing sitemap-0.xml                                                                           | ✓ VERIFIED | Single `<sitemap><loc>https://studiobluemli.com/sitemap-0.xml</loc></sitemap>` entry.                                                                                                                                            |
| `dist/client/sitemap-0.xml`                     | 11 `<loc>` entries on apex                                                                                              | ✓ VERIFIED | 11 entries verified by `grep -oE '<loc>' \| wc -l` = 11; all `https://studiobluemli.com/...`.                                                                                                                                    |
| `scripts/check-seo-output.mjs` (NEW)            | Production smoke test asserting robots.txt + canonical trailing slash + preview-aware per-piece og:image                | ✓ VERIFIED | New file, 123 lines. Wired into `npm run ci:seo-check`. End-to-end run prints 4× `OK:` lines and exits 0.                                                                                                                        |
| `package.json:scripts.deploy`                   | Prefixed with `PUBLIC_DEPLOY_ENV=production`                                                                            | ✓ VERIFIED | Line 12: `"deploy": "PUBLIC_DEPLOY_ENV=production astro build && wrangler deploy"`. `deploy:preview` (line 13) intentionally NOT prefixed.                                                                                       |
| `package.json:scripts.ci:seo-check` (NEW)       | Production-flagged build + smoke test                                                                                   | ✓ VERIFIED | Line 18: `"ci:seo-check": "PUBLIC_DEPLOY_ENV=production astro build && node scripts/check-seo-output.mjs"`. Exits 0 in this verification run.                                                                                    |
| `src/components/design-skill/About.jsx:50`      | `<Mark.Heart color="var(--coral-500)" filled={false} />` (outline ♡)                                                    | ✓ VERIFIED | Line 50 verbatim contains `filled={false}`. Built about/index.html: `♡`=1, `♥`=0.                                                                                                                                                |
| `astro.config.mjs`                              | Sitemap integration + Fonts API                                                                                         | ✓ VERIFIED | (Unchanged from prior verification; integrations include `react()` and `sitemap()`.)                                                                                                                                             |

### Key Link Verification

| From                                                                                  | To                                                  | Via                                              | Status     | Details                                                                                                                                                                                                |
| ------------------------------------------------------------------------------------- | --------------------------------------------------- | ------------------------------------------------ | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `src/pages/index.astro`                                                               | `splitPopups()` + `<PopupCallout />`                | import                                           | ✓ WIRED    | Imports `splitPopups` from `../lib/popups`; renders `<PopupCallout />` conditionally on `hasUpcoming` (correctly omitted on zero popups today).                                                          |
| `src/pages/popups.astro`                                                              | `splitPopups()` + `astro:content render()`          | import                                           | ✓ WIRED    | Soonest description rendered via `astro:content`'s `render()` → Content component.                                                                                                                       |
| All 6 prerendered surfaces                                                            | `<SEO />`                                           | `<SEO slot="head" ... />`                        | ✓ WIRED    | index, about, gallery, popups, say-hi, gallery/[slug] all consume SEO.astro; canonical+og:image+og:url confirmed in built HTML for all.                                                                  |
| `src/pages/robots.txt.ts`                                                             | `isProduction()`                                    | import                                           | ✓ WIRED    | Import in place; production-flagged build path is now reachable via `package.json:scripts.deploy` (and `ci:seo-check`); built robots.txt confirms Allow branch fires.                                    |
| `src/components/SEO.astro`                                                            | `resolveCanonicalBase(Astro.site)`                  | import                                           | ✓ WIRED    | Returns apex; SEO.astro now appends `'/'` on root path explicitly (GAP-02 fix).                                                                                                                          |
| `src/pages/gallery/[slug].astro` (Phase 2)                                            | `resolveAssetBase(Astro.site)`                      | import                                           | ✓ WIRED    | Smoke test confirms second-build with `PUBLIC_CF_WORKERS_URL` set propagates the preview hostname into per-piece og:image while canonical stays on apex.                                                  |
| `src/pages/about.astro`                                                               | gallery hero WebPs                                  | `/gallery/<slug>/hero-800.webp` URLs              | ✓ WIRED    | 3 img tags with cluster-blush, cluster-cobalt, cluster-coral hero-800.webp in built HTML.                                                                                                                |
| `src/pages/say-hi.astro`                                                              | IG DM URL + mailto                                  | `site.ig_dm_url` + `mailto:${site.contact_email}` | ✓ WIRED    | Both `https://ig.me/m/studiobluemli` and `mailto:hi@studiobluemli.com` present and clickable.                                                                                                            |
| `scripts/check-seo-output.mjs`                                                        | `dist/client/robots.txt`                            | fs.readFileSync + string includes assert         | ✓ WIRED    | Run-time: emits `OK: GAP-01 / BL-03 — robots.txt has Allow + Sitemap (no Disallow).`                                                                                                                     |
| `scripts/check-seo-output.mjs`                                                        | `dist/client/index.html`                            | fs.readFileSync + regex assert                   | ✓ WIRED    | Run-time: emits `OK: GAP-02 / BL-01 — homepage canonical has trailing slash.`                                                                                                                            |
| `scripts/check-seo-output.mjs`                                                        | `dist/client/gallery/cluster-coral/index.html`      | second build w/ PUBLIC_CF_WORKERS_URL set         | ✓ WIRED    | Run-time: emits two `OK:` lines for GAP-03 (og:image uses preview hostname; canonical stays on apex).                                                                                                    |
| `package.json:scripts.deploy`                                                         | `src/pages/robots.txt.ts` via `isProduction()`      | `PUBLIC_DEPLOY_ENV=production` env-var inline    | ✓ WIRED    | Vite inlines the var into `import.meta.env.PUBLIC_DEPLOY_ENV`; `isProduction()` reads it; robots.txt body flips to Allow + Sitemap.                                                                       |
| `src/components/design-skill/About.jsx`                                               | `src/components/design-skill/Mark.jsx`              | imported and invoked with `filled={false}`       | ✓ WIRED    | About.jsx line 50 passes `filled={false}` → Mark.Heart renders `♡`; built about/index.html confirms.                                                                                                     |

### Data-Flow Trace (Level 4)

| Artifact                            | Data Variable               | Source                                                                                        | Produces Real Data                                                                                                                                                                                                | Status                       |
| ----------------------------------- | --------------------------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------- |
| `src/pages/index.astro`             | `featuredSorted` (3 pieces) | `getCollection('gallery')` → filter `featured===true` → sort by `published_at` desc → slice 3 | ✓ Yes — 6 gallery entries on disk; built HTML shows cluster-blush, cluster-cobalt, cluster-coral hero-400.webp                                                                                                    | ✓ FLOWING                    |
| `src/pages/index.astro`             | `{soonest, hasUpcoming}`    | `getCollection('popups')` → `splitPopups(...)`                                                | Currently empty (popups collection is `.gitkeep` only) — by D-03 design, mini-callout is OMITTED. Built HTML has 0 rendered popup-callout sections.                                                              | ✓ FLOWING (empty-state path) |
| `src/pages/popups.astro`            | `soonest/alsoComing/past`   | Same `splitPopups` pipeline                                                                    | Currently empty → D-08 empty state rendered (`"No pop-ups on the calendar yet — follow @studiobluemli for the next one."`)                                                                                       | ✓ FLOWING (empty-state path) |
| `src/pages/about.astro`             | `stripPieces` (3 entries)   | gallery collection → featured filter → sort → slice 3 → `{slug, name}`                        | ✓ Yes — same 3 slugs as landing                                                                                                                                                                                  | ✓ FLOWING                    |
| `src/pages/say-hi.astro`            | `site.ig_dm_url`, `site.contact_email` | `getEntry('site','default').data`                                                       | ✓ Yes — config.yaml populates both                                                                                                                                                                                | ✓ FLOWING                    |
| `src/components/SEO.astro`          | `canonical`, `ogImageAbs`, etc. | site config + props + `Astro.site` + `resolveCanonicalBase`                                | ✓ Real, correct values: canonical includes trailing slash on root, apex on all pages (GAP-02 + SC5 satisfied); og:image is `${canonicalBase}/og-default.png` by default, overridable to preview hostname per page. | ✓ FLOWING                    |
| `src/pages/robots.txt.ts`           | body string                 | `isProduction()` reading PUBLIC_DEPLOY_ENV from `process.env` + `import.meta.env`             | ✓ Real value flows: `npm run deploy` (and `ci:seo-check`) set `PUBLIC_DEPLOY_ENV=production`, so `isProduction()` returns `true` and body is Allow + Sitemap. Verified end-to-end by the smoke test.              | ✓ FLOWING                    |
| `src/pages/gallery/[slug].astro`    | per-piece `og:image` URL   | `resolveAssetBase(Astro.site)` reading `PUBLIC_CF_WORKERS_URL` from both env surfaces         | ✓ Real value flows: smoke test second build emits `og:image content="https://preview.example.workers.dev/gallery/cluster-coral/hero-800.webp"`; default (no env var) keeps apex.                                  | ✓ FLOWING                    |
| `src/components/design-skill/About.jsx` | signature heart glyph    | `Mark.Heart filled={false}` → emits `♡`                                                       | ✓ Real value flows: built about/index.html has `♡` (U+2661) in the signature inline-flex span at coral-500.                                                                                                       | ✓ FLOWING                    |

### Behavioral Spot-Checks

| Behavior                                                                | Command                                                                                                                | Result                                                                                | Status   |
| ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- | -------- |
| Built homepage canonical has trailing slash                             | `grep -c 'rel="canonical" href="https://studiobluemli.com/"' dist/client/index.html`                                   | 1                                                                                     | ✓ PASS   |
| Built homepage og:url has trailing slash                                | `grep -oE 'og:url"[^>]+content="[^"]+"' dist/client/index.html`                                                        | `og:url" content="https://studiobluemli.com/"`                                        | ✓ PASS   |
| Built robots.txt has Sitemap reference                                  | `grep -c 'Sitemap:' dist/client/robots.txt`                                                                            | 1                                                                                     | ✓ PASS   |
| Built robots.txt has Allow rather than Disallow                         | `grep -c 'Allow:' dist/client/robots.txt; grep -c 'Disallow:' dist/client/robots.txt`                                  | 1; 0                                                                                  | ✓ PASS   |
| About signature has outline ♡                                           | `grep -c '♡' dist/client/about/index.html`                                                                             | 1                                                                                     | ✓ PASS   |
| About has zero filled ♥                                                 | `grep -c '♥' dist/client/about/index.html`                                                                             | 0                                                                                     | ✓ PASS   |
| About has zero press placeholders                                       | `grep -ciE 'press\|featured in\|as seen in' dist/client/about/index.html`                                              | 0                                                                                     | ✓ PASS   |
| About renders 3 photo strip cells (hero-800.webp)                       | `grep -oE '/gallery/[a-z-]+/hero-800.webp' dist/client/about/index.html`                                               | cluster-blush, cluster-cobalt, cluster-coral                                          | ✓ PASS   |
| /say-hi has zero form/input                                             | `grep -cE '<form\|<input' dist/client/say-hi/index.html`                                                               | 0                                                                                     | ✓ PASS   |
| /say-hi has IG DM + mailto                                              | `grep -oE 'https://ig\.me/m/studiobluemli\|mailto:hi@studiobluemli\.com' dist/client/say-hi/index.html`                | both present                                                                          | ✓ PASS   |
| /popups empty-state copy                                                | `grep -oE 'No pop-ups on the calendar' dist/client/popups/index.html`                                                  | match                                                                                 | ✓ PASS   |
| sitemap-0.xml has 11 `<loc>` entries                                    | `grep -oE '<loc>' dist/client/sitemap-0.xml \| wc -l`                                                                  | 11                                                                                    | ✓ PASS   |
| sitemap-index references sitemap-0.xml                                  | `grep -c 'sitemap-0.xml' dist/client/sitemap-index.xml`                                                                | 1                                                                                     | ✓ PASS   |
| All canonicals on apex (no preview leak)                                | `grep -E 'rel="canonical"' dist/client/{index,about,gallery,popups,say-hi,gallery/cluster-coral}/index.html`           | all `https://studiobluemli.com[/path]`                                                | ✓ PASS   |
| Gallery index has 6 pieces                                              | `grep -oE 'href="/gallery/cluster-[a-z]+"' dist/client/gallery/index.html \| sort -u \| wc -l`                         | 6                                                                                     | ✓ PASS   |
| `npm run ci:seo-check` end-to-end                                       | `rm -rf dist && npm run ci:seo-check`                                                                                  | Exits 0; prints 4× `OK:` lines + `All SEO smoke checks passed.`                       | ✓ PASS   |
| `npm run ci:brand-check`                                                | `npm run ci:brand-check`                                                                                               | `All brand rules pass.`                                                               | ✓ PASS   |
| `npm run ci:lowercase-check`                                            | `npm run ci:lowercase-check`                                                                                           | `All src/pages/ filenames are lowercase.`                                             | ✓ PASS   |
| TZ logic: today=2026-05-14 LA, popup_date=2026-05-13 → past             | Inline Temporal compare via `popups.ts`                                                                                | `compare < 0` = true                                                                  | ✓ PASS   |

### Requirements Coverage

| Requirement | Source Plan          | Description                                                                                                | Status      | Evidence                                                                                                                                                                            |
| ----------- | -------------------- | ---------------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| PAG-01      | 03-01, 03-03          | Landing renders hero, next-popup callout (empty-state per D-03 = OMIT), 3-6 featured pieces, footer        | ✓ SATISFIED | Built index.html confirms hero + 3 featured + footer + correct empty-state omission.                                                                                                |
| PAG-02      | 03-02                 | Gallery page renders the grid                                                                              | ✓ SATISFIED | Phase 2 deliverable; SEO.astro wired into gallery.astro + gallery/[slug].astro; sitemap-0.xml confirms gallery routes; 6 pieces in built gallery/index.html.                          |
| PAG-03      | 03-03                 | /popups renders upcoming + past; TZ-correct LA cutoff at build time                                        | ✓ SATISFIED | `splitPopups` uses Temporal.PlainDate in `America/Los_Angeles`; popups page renders empty state today; Plan 03 SUMMARY documents seeded-state working.                                |
| PAG-04      | 03-05                 | Cloudflare cron daily rebuild                                                                              | ⏸ DEFERRED  | Spike-FAIL; PAG-04 explicitly deferred per REQUIREMENTS.md + ROADMAP.md. Not a Phase 3 gap. See 03-05-SUMMARY.md.                                                                    |
| PAG-05      | 03-04, 03-07          | About renders portrait + signature + 1–3 photo strip, no founder face, no empty press                      | ✓ SATISFIED | 3-paragraph portrait + 3 photo strip cells + outline ♡ signature (GAP-04 closed via 03-07); zero press placeholders.                                                                |
| PAG-06      | 03-02 (narr.), 03-04  | Say Hi renders IG DM link + mailto fallback, no form                                                       | ✓ SATISFIED | Built say-hi.html has 0 form/0 input, IG DM + mailto present.                                                                                                                       |
| PAG-07      | 03-02, 03-06          | Shared SEO emits per-page title/description/og/twitter/canonical-to-apex                                   | ✓ SATISFIED | All meta tags emitted; canonical points to apex; root canonical includes trailing slash (GAP-02 closed); per-piece og:image preview-aware (GAP-03 closed).                          |
| PAG-08      | 03-02, 03-06          | @astrojs/sitemap generates sitemap-index.xml; robots.txt references the sitemap                            | ✓ SATISFIED | sitemap-index + sitemap-0.xml verified (11 routes). robots.txt now includes `Sitemap: https://studiobluemli.com/sitemap-index.xml` on production deploy (GAP-01 closed).             |
| PAG-09      | 03-04                 | All product images have alt text; never uses flower/petal/floral/bloom/blossom                             | ✓ SATISFIED | Phase 2 Zod schema requires alt; about photo strip uses piece names; brand-CI grep enforces vocabulary. `npm run ci:brand-check` reports `All brand rules pass.`                      |

All 8 active Phase 3 requirements satisfied; PAG-04 deferred to a future phase per ROADMAP.

### Anti-Patterns Found (Code-Review Notes)

The refreshed `03-REVIEW.md` (post-gap-closure) reports 0 BLOCKERs, 5 WARNINGs. Summarized here so the next-phase planner can see them — none gate Phase 3 closure.

| File                                                                                                  | Pattern                                                                                                                              | Severity     | Impact                                                                                                                                                                     |
| ----------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/lib/site-url.ts:74-80`                                                                           | `??` precedence in `resolveAssetBase()` short-circuits on `null/undefined` but NOT on empty strings (WR-01)                          | ⚠️ Warning   | Latent; CI env-var-set-but-blank edge case; smoke test would not catch.                                                                                                    |
| `src/components/SEO.astro:32, 41`                                                                     | `pathname` prop unvalidated — empty string or missing-leading-slash still produce malformed canonicals (WR-02)                       | ⚠️ Warning   | Latent; every internal caller passes a well-formed path today. Same bug shape as GAP-02 just at a different layer.                                                          |
| `package.json:12, 18`                                                                                 | `deploy` and `ci:seo-check` skip `astro check` (run only `astro build`) (WR-03)                                                      | ⚠️ Warning   | Typecheck errors can theoretically slip to production. Mitigated by `build` script having `astro check && astro build`.                                                    |
| `scripts/check-seo-output.mjs:26`                                                                     | Hard-coded `PIECE_SLUG = 'cluster-coral'` — silent skew if that slug ever renamed (WR-04)                                            | ⚠️ Warning   | If `cluster-coral` is ever deleted, the test fails noisily (which is fine), but a rename without updating the script would skip the GAP-03 assertion entirely.              |
| `scripts/check-seo-output.mjs:82-85`                                                                  | Second build failure increments `failures` but execution continues; downstream errors mask the root cause (WR-05)                    | ⚠️ Warning   | Test output less actionable on a corrupted build, but the gate still fails (non-zero exit) — no soft-pass risk.                                                            |

**Carried over from the original 03-REVIEW (still open as non-blocking):** WR-01 multi-day popups in callout/list, WR-02 `tz` field unused in `todayInLA()`, WR-03 Footer `target=_blank` without noopener, WR-04 non-null assertion on `getEntry('site','default')`, WR-05 `dangerouslySetInnerHTML` in Header, WR-07 slug not URL-encoded, WR-08 og-default contrast ~3.2:1, WR-09 en-dash collision in PopupCallout times, IN-01 PopupStrip uses `new Date()` instead of Temporal, IN-02 hero eyebrow `whiteSpace: nowrap` may overflow at 320px. All are tracked in 03-REVIEW.md history; none gate Phase 3 closure.

**Minor consistency note (informational, not a gap):** Non-root pages emit canonical without trailing slash (e.g., `https://studiobluemli.com/about`), while sitemap-0.xml emits the slashed form (`https://studiobluemli.com/about/`). The gap-closure smoke test was deliberately relaxed to accept either form on non-root pages (per 03-06-SUMMARY decision 4); the homepage trailing-slash invariant — the only one ROADMAP SC5 + GAP-02 actually demanded — IS enforced. This non-root canonical/sitemap inconsistency is recorded as WR-02 in the refreshed REVIEW and is non-blocking.

### Human Verification Required

See `human_verification:` in frontmatter — 4 items routed to human:

1. **Unfurl preview verification** for `https://studiobluemli.com/` in iMessage/Slack/IG DM — each platform should render the correct card with og-default.png after the BL-01/BL-03 fixes land in production.
2. **Per-piece og:image unfurl** for `https://studiobluemli.com/gallery/cluster-coral/` — verify the hero photo loads in the unfurl card (not the default mark).
3. **320px viewport eyebrow overflow check** (IN-02 from original 03-REVIEW; still open as known warning).
4. **Production /robots.txt + /sitemap-index.xml curl** after `npm run deploy` — assert the Allow + Sitemap body and the full sitemap-index → sitemap-0.xml chain on the actual deployed apex.

### Gaps Summary

**All four prior gaps closed.** Phase 3 is structurally complete: every page renders its real content, SEO infrastructure is hardened, and a CI gate (`npm run ci:seo-check`) prevents the SEO regressions from re-landing.

- **GAP-01 (BL-03 / robots.txt)** — closed in plan 03-06, commit `a11e118`. Deploy script now sets `PUBLIC_DEPLOY_ENV=production` so production builds emit `Allow + Sitemap`. Preview deploys (`deploy:preview`) intentionally keep `Disallow: /`.
- **GAP-02 (BL-01 / homepage canonical)** — closed in plan 03-06, commit `5c67814`. `SEO.astro` now appends a trailing slash on root so canonical, og:url, sitemap entry, and the URL users actually visit all agree on `https://studiobluemli.com/`.
- **GAP-03 (BL-02 / per-piece og:image)** — closed in plan 03-06, commits `5c67814` + `a11e118`. `resolveAssetBase()` reads from both `process.env` and `import.meta.env.PUBLIC_*` aliases, fixing the Vite-strips-non-PUBLIC-vars defect. Smoke test verifies preview-hostname propagation while canonical stays on apex.
- **GAP-04 (WR-06 / About signature heart)** — closed in plan 03-07, commit `9079326`. `Mark.Heart filled={false}` now renders outline `♡` per D-16.

**Re-verification evidence (this run):**
- `dist/client/robots.txt` contains `Allow: /` + `Sitemap: https://studiobluemli.com/sitemap-index.xml` (production-flagged build).
- `dist/client/index.html` canonical and og:url both `https://studiobluemli.com/` (with trailing slash).
- `dist/client/gallery/cluster-coral/index.html` (preview-hostname build) carries `og:image content="https://preview.example.workers.dev/gallery/cluster-coral/hero-800.webp"` while canonical remains apex.
- `dist/client/about/index.html` has 1×`♡` and 0×`♥`.
- `npm run ci:seo-check` exits 0 with the 4 expected `OK:` lines.
- `npm run ci:brand-check` and `npm run ci:lowercase-check` both pass.

**Status: human_needed** — the 5 success criteria are automatically verified. 4 human items remain for in-platform unfurl confirmation and production-deploy curl validation. Per the GSD decision tree these route to human review (Step 8); they're not gaps.

PAG-04 (daily cron rebuild) remains correctly **DEFERRED** to a future phase per ROADMAP/REQUIREMENTS — the integrated `wrangler.jsonc` approach is structurally incompatible with `@astrojs/cloudflare@13.5`, and the documented fallback (separate cron-only Worker) will land later. Phase 3's TZ-correct build-time bucketing — the half of SC2 that is in scope — is verified.

---

_Verified: 2026-05-14T16:00:00Z_
_Verifier: Claude (gsd-verifier)_
_Re-verification after gap-closure wave (plans 03-06 SEO/robots + 03-07 heart glyph)_
