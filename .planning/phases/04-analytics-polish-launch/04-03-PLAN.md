---
phase: 04-analytics-polish-launch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - SETUP-DNS.md
  - CONTENT_EDITING.md
autonomous: true
requirements: [FND-03, LCH-07]
tags: [docs, dns, cutover, founder-facing]

must_haves:
  truths:
    - "SETUP-DNS.md exists at the repo root with 5 numbered steps walking the founder through Cloudflare's Custom Domain + Redirect Rule + env-var configuration"
    - "Voice matches CONTENT_EDITING.md (sentence-case verb-led headings, numbered steps, bold UI labels, no jargon, no emoji)"
    - "The doc explicitly does NOT submit to hstspreload.org (deferred per CONTEXT.md)"
    - "astro.config.mjs's `site:` is verified to be `'https://studiobluemli.com'` (D-07; no edit if already correct)"
    - "public/og-default.png is verified to be 1200x630 cream-background brand-faithful (D-10; 1-line discretion check)"
    - "CONTENT_EDITING.md links to SETUP-DNS.md in a 'Going live' or 'Operations' register block (cross-reference so the founder can find it)"
  artifacts:
    - path: "SETUP-DNS.md"
      provides: "Founder-facing one-time DNS cutover walkthrough"
      min_lines: 80
      contains: "Going live with studiobluemli.com"
    - path: "CONTENT_EDITING.md"
      provides: "Cross-reference link to SETUP-DNS.md"
      contains: "SETUP-DNS.md"
  key_links:
    - from: "CONTENT_EDITING.md"
      to: "SETUP-DNS.md"
      via: "markdown link"
      pattern: "SETUP-DNS\\.md"
---

<objective>
Create the founder-facing `SETUP-DNS.md` at the repo root that walks the 5 Cloudflare dashboard steps for the DNS cutover (D-06), and verify the two read-only preconditions for cutover (D-07: `astro.config.mjs` `site:` is the apex; D-10: `public/og-default.png` is brand-faithful). Register the new doc in `CONTENT_EDITING.md` so the founder can find it.

Purpose: Close FND-03 (apex resolves with HTTPS, www→apex 301) and LCH-07 (DNS cutover with valid cert) by giving the founder a non-engineer playbook. The doc is the only deliverable that the founder will read and execute alone — voice, register, and concrete UI labels matter more than completeness.

Output: `SETUP-DNS.md` at repo root (founder-walked, no jargon); a small cross-reference paragraph in `CONTENT_EDITING.md`; written verification (in the SUMMARY) of the two read-only preconditions D-07 and D-10.

Note on scope: This plan does NOT execute the cutover. The cutover happens in Plan 05 (Wave 2) when the founder walks `SETUP-DNS.md` end-to-end with Claude observing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analytics-polish-launch/04-CONTEXT.md
@.planning/phases/04-analytics-polish-launch/04-RESEARCH.md
@.planning/phases/04-analytics-polish-launch/04-PATTERNS.md

@CONTENT_EDITING.md
@astro.config.mjs
@.claude/skills/studio-bluemli-design/SKILL.md
@scripts/check-brand-rules.sh
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder → Cloudflare dashboard | Founder is the only human with credentials. SETUP-DNS.md is a script she follows; the wrong instruction makes her click the wrong button. |
| `SETUP-DNS.md` → Cloudflare control plane | Doc accuracy is the only safeguard: a misnamed UI element makes her give up or pick the wrong option. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Tampering | Founder mis-routes the apex to a different Cloudflare resource (e.g., a stale Pages project instead of the Workers project) | mitigate | SETUP-DNS.md Step 1 names the exact navigation: **Workers & Pages → studio-bluemli (the worker) → Settings → Domains & Routes → Add → Custom Domain**. The worker name `studio-bluemli` is the disambiguator. |
| T-04-19 | Information Disclosure | Cert mis-issuance during the brief window between Custom Domain add and cert validation (Pitfall 5 in RESEARCH.md) | accept | Cloudflare auto-provisions the cert via ACME; the window is 1–5 minutes. Step 4 of SETUP-DNS.md explicitly instructs the founder to wait and NOT refresh devtools; Plan 05 launch-checklist item 8 (`curl -vI https://studiobluemli.com`) explicitly verifies a valid cert chain BEFORE declaring cutover complete. |
| T-04-20 | Tampering | HSTS preload-list submission accidentally happens before 30-day clean window (Pitfall 3 in RESEARCH.md) | mitigate | SETUP-DNS.md explicitly tells the founder NOT to submit to hstspreload.org. The header ships with the `preload` directive (eligible) but the registration step is deferred. The doc carries a one-line "deferred to v1.x" note. |
| T-04-21 | Spoofing | `PUBLIC_UMAMI_WEBSITE_ID` mistakenly set as a Cloudflare Secret (encrypted, not exposed to Vite/Astro) → Umami snippet's `data-website-id` is undefined at build time → snippet omitted → zero events | mitigate | SETUP-DNS.md Step 3 says explicitly **"Set the type to Plaintext (not Secret — this is a public identifier)"**. Plan 05 launch checklist item 5 catches the symptom (zero Realtime events) within 5 minutes if this is wrong. |
| T-04-22 | Tampering | DNS one-way cutover risk (cert provisioning fails, www→apex rule misconfigured, Worker custom domain stale) | mitigate | The doc is sequenced so cert work happens AT THE START (Step 1 — auto-provisions during the founder's coffee break) while the founder configures the Redirect Rule (Step 2) and env var (Step 3) in parallel. Step 5 explicitly hands off to Plan 05's launch checklist, which is the cutover-completeness gate. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify two read-only preconditions and write SETUP-DNS.md</name>
  <files>SETUP-DNS.md, CONTENT_EDITING.md</files>
  <read_first>
    - CONTENT_EDITING.md (voice/register reference — read in full)
    - .planning/phases/04-analytics-polish-launch/04-RESEARCH.md (§Code Examples "SETUP-DNS.md skeleton (D-06)" lines 635-699 — the canonical text to refine)
    - astro.config.mjs (line 8 — verify `site: 'https://studiobluemli.com'`)
    - scripts/check-brand-rules.sh lines 50-60 (regex for `flower|petal|floral|bloom|blossom` enforcement — applies to repo-root .md files too? verify by reading the script)
  </read_first>
  <action>
**Sub-step A — Verify D-07 (read-only):**
Run: `grep -nE "^[[:space:]]*site:" astro.config.mjs`. Expected output: `8:  site: 'https://studiobluemli.com',`. If this expected line is present, record in the SUMMARY: "D-07 confirmed: astro.config.mjs:8 site='https://studiobluemli.com'." If absent or different, STOP and surface the discrepancy — do NOT edit `astro.config.mjs` in this plan (Phase 3 D-22 + D-26 own that file).

**Sub-step B — Verify D-10 (read-only):**
Run: `file public/og-default.png` to confirm dimensions. Expected: `PNG image data, 1200 x 630, 8-bit/color RGB...` or similar 1200×630 PNG. Run: `ls -la public/og-default.png` to confirm presence. Open the file (`open public/og-default.png` if on macOS — REQUIRED, not optional; the visual check below is the D-10 gate) and visually verify: cream background (NOT white), logo lockup or wordmark, no flower iconography, no center-bead motif.

**Required SUMMARY attestation** (this is the D-10 gate — without this line, D-10 is not closed):

The SUMMARY MUST contain a verbatim attestation line of the form:
- `D-10 visual check: cream background = yes, flower iconography = no, logo lockup/wordmark = [present|absent]`

Pick `present` or `absent` based on what you actually see. If cream background is `no` OR flower iconography is `yes`, the file fails D-10 — do NOT regenerate in this plan (out of scope — flag it for Plan 05 to address with a one-off script run using `scripts/generate-og-default.mjs` which already exists in the repo).

Also record (separately from the attestation line) one of:
- "D-10 confirmed: 1200×630 PNG, cream background, [logo lockup OR wordmark], no flower vocabulary." OR
- "D-10 FLAG: og-default.png is [white/wrong-dimensions/has-flower-iconography] — requires regeneration in Plan 05 before cutover."

**Sub-step C — Create `SETUP-DNS.md` at the repo root:**

Verify the file does not already exist (`ls SETUP-DNS.md 2>&1` should return "No such file or directory"). Then create it with the content below. The body follows the canonical skeleton from RESEARCH.md §Code Examples lines 635-699 — refined for voice consistency with CONTENT_EDITING.md (sentence-case verb-led headings, bold UI labels, plain prose, no emoji).

```markdown
# Going live with studiobluemli.com

This is a one-time setup. It takes about 15 minutes. You only do this when the
site is ready for the public. Once it's done, you don't touch it again.

Everything below happens in your Cloudflare account. You'll need to be signed in.

***

## Step 1 — Point studiobluemli.com at the site

1. Go to Cloudflare and open **Workers & Pages**.
2. Click the **studio-bluemli** worker in the list.
3. Click **Settings** at the top.
4. Find the **Domains & Routes** section and click **Add**, then **Custom Domain**.
5. Type `studiobluemli.com` (no `https`, no slashes — just the name).
6. Click **Add Custom Domain**.

Cloudflare creates the DNS record and starts issuing the HTTPS certificate. Move
on to Step 2 — the certificate finishes on its own in a few minutes.

***

## Step 2 — Redirect www.studiobluemli.com to studiobluemli.com

1. Go to Cloudflare and open the **studiobluemli.com** zone (the domain itself,
   not the worker).
2. Click **Rules** in the left sidebar, then **Redirect Rules**.
3. Click **Create rule**.
4. Name the rule `www-to-apex` (any name works).
5. Under **When incoming requests match**, choose **Wildcard pattern**.
6. In the request URL field, paste: `http*://www.studiobluemli.com${URI}`
7. Under **Then…**, set **Target URL** to: `https://studiobluemli.com${URI}`
8. Set the **Status code** to **301**.
9. Click **Deploy**.

The `${URI}` part keeps the path — so `www.studiobluemli.com/gallery` lands on
`studiobluemli.com/gallery`, not the homepage.

***

## Step 3 — Tell the worker your Umami website ID

1. Back in **Workers & Pages**, click **studio-bluemli** → **Settings**.
2. Scroll to **Variables and Secrets**.
3. Click **Add**.
4. Set the variable name to: `PUBLIC_UMAMI_WEBSITE_ID`
5. Set the value to the website ID from your Umami Cloud dashboard
   (Umami Cloud → Settings → Websites → studiobluemli.com → copy the ID).
6. Set the type to **Plaintext** — not Secret. This is a public identifier,
   not a credential.
7. Save.
8. Trigger a fresh deploy so the new variable takes effect. The simplest way:
   in the **studio-bluemli** worker page, click **Deployments** at the top and
   then **Trigger deploy**.

***

## Step 4 — Wait

Take a coffee break. Cloudflare is finishing the HTTPS certificate. This usually
takes 1–5 minutes. Refreshing won't make it faster.

***

## Step 5 — Check that it all works

When the certificate is ready, you can open `https://studiobluemli.com` and you'll
see the live site. Claude walks a checklist with you to confirm everything works:
sitemap, robots, link previews, Umami events, Lighthouse scores. You only need
to do three checks on your phone (Claude will tell you when):

1. Tap the **DM me on Instagram** button on `/say-hi` — does Instagram open?
2. Tap the **email** link on `/say-hi` — does your email app open with the
   address filled in?
3. Open `studiobluemli.com` over cellular (not wifi) — does it feel fast?

That's it.

***

## What is not done here

A small, browser-level safety feature called HSTS preload makes the site refuse
to ever speak plain HTTP, even on visitors' first visit. The site ships ready for
it, but registering it with browser vendors is intentionally skipped right now —
it's effectively a one-year commitment once submitted. Revisit after the site
has been live for 30 days without issue.

***

*Last updated: 2026-05-15. Filed under operations.*
```

**Brand-rule guards while writing:**
- No occurrence of `white` as a CSS value (the words "white" in regular sentences are fine — the check-brand-rules.sh regex is `bg-white|background:\s*white`, not the bare word). Read `scripts/check-brand-rules.sh` lines 22-28 to confirm pattern.
- No occurrence of `flower`, `petal`, `floral`, `bloom`, or `blossom` anywhere (line 53 of the script matches `--include='*.astro' --include='*.jsx' --include='*.tsx' --include='*.ts' --include='*.css'` — `.md` files appear NOT to be scanned, but write defensively).
- Sentence-case headings throughout.
- No emoji.

**Sub-step D — Cross-reference in CONTENT_EDITING.md:**

Open `CONTENT_EDITING.md`. Find a logical spot near the end (or before the existing "Frontmatter reference card" section if present) and append a short Operations section. Use the EXACT text below — it's deliberately short and matches the existing CONTENT_EDITING.md register:

```markdown

***

## Operations

When the site is ready for the public and you want to put it on `studiobluemli.com`,
follow the one-time steps in [SETUP-DNS.md](SETUP-DNS.md). It's a 15-minute walk-through
of the Cloudflare dashboard. You only do it once.
```

Place this AFTER the last existing top-level `##` section and BEFORE any final separator or footer if present. If unsure where to put it, append at the end of the file with a leading `---` separator.
  </action>
  <verify>
    <automated>test -f SETUP-DNS.md && wc -l SETUP-DNS.md && grep -c '^## Step' SETUP-DNS.md && grep -c 'PUBLIC_UMAMI_WEBSITE_ID' SETUP-DNS.md && grep -c 'studio-bluemli' SETUP-DNS.md && grep -c 'SETUP-DNS.md' CONTENT_EDITING.md && grep -cE 'flower|petal|floral|bloom|blossom' SETUP-DNS.md CONTENT_EDITING.md && bash scripts/check-brand-rules.sh && file public/og-default.png</automated>
  </verify>
  <acceptance_criteria>
    - `test -f SETUP-DNS.md` exits 0.
    - `wc -l SETUP-DNS.md` returns at least 70 lines and no more than 130 (sized like CONTENT_EDITING.md sections).
    - `grep -c '^## Step' SETUP-DNS.md` returns `5` (Steps 1 through 5).
    - `grep -c 'PUBLIC_UMAMI_WEBSITE_ID' SETUP-DNS.md` returns `1` (env-var name in Step 3).
    - `grep -c 'studio-bluemli' SETUP-DNS.md` returns at least `3` (the worker name appears in Steps 1, 3, and the trigger-deploy line).
    - `grep -c 'Plaintext' SETUP-DNS.md` returns `1` (Step 3's explicit non-Secret instruction).
    - `grep -cE 'flower|petal|floral|bloom|blossom' SETUP-DNS.md CONTENT_EDITING.md` returns `0` (no flower vocabulary).
    - `grep -c 'SETUP-DNS\.md' CONTENT_EDITING.md` returns at least `1` (cross-reference present).
    - `grep -c '^## Operations' CONTENT_EDITING.md` returns `1` (the new section header).
    - `bash scripts/check-brand-rules.sh` exits 0 (no brand-rule regressions in any file).
    - `file public/og-default.png` reports `1200 x 630` (D-10 verified) — record the actual output in SUMMARY.
    - **D-10 attestation gate (W-5):** SUMMARY MUST contain the verbatim attestation line `D-10 visual check: cream background = yes, flower iconography = no, logo lockup/wordmark = [present|absent]` (executor fills the bracketed value). If `cream background = no` OR `flower iconography = yes`, D-10 is FLAGGED for Plan 05 regeneration and the line MUST reflect the actual observation. Without this attestation line in the SUMMARY, D-10 is NOT closed regardless of other checks.
    - `grep -c 'D-10 visual check: cream background' .planning/phases/04-analytics-polish-launch/04-03-SUMMARY.md` returns `1` (attestation present after SUMMARY is written).
    - `grep -nE "^[[:space:]]*site:" astro.config.mjs` returns `8:  site: 'https://studiobluemli.com',` (D-07 verified).
    - No occurrence of the word "emoji" or any emoji character in SETUP-DNS.md (`grep -cE '[😀-🙏]|[🚀-🛿]|[✓✗✘✔]' SETUP-DNS.md` returns `0`).
  </acceptance_criteria>
  <done>
    `SETUP-DNS.md` exists at repo root with 5 numbered Cloudflare-dashboard steps in CONTENT_EDITING.md voice. CONTENT_EDITING.md links to it via an `## Operations` section. D-07 and D-10 are both verified (or D-10 flagged for Plan 05) with the required attestation line in SUMMARY. No brand-rule violations.
  </done>
</task>

</tasks>

<verification>
- `SETUP-DNS.md` at repo root contains all 5 numbered Cloudflare steps (Custom Domain, Redirect Rule, env var, Wait, founder phone checks).
- The doc passes `bash scripts/check-brand-rules.sh` (or is excluded from its scan; either way no failures).
- `CONTENT_EDITING.md` has an `## Operations` section linking to `SETUP-DNS.md`.
- Sub-step A verified `astro.config.mjs:8 site='https://studiobluemli.com'`.
- Sub-step B verified `public/og-default.png` is 1200×630 cream-background (or flagged for Plan 05 with the exact symptom), AND the verbatim D-10 attestation line is present in SUMMARY.
- No emoji characters anywhere in either doc.
</verification>

<success_criteria>
1. Founder can read `SETUP-DNS.md` end-to-end without a developer translating any term.
2. The 5 Cloudflare dashboard steps are concrete enough to follow in <15 minutes.
3. The doc explicitly says "Plaintext (not Secret)" for the Umami env var — mitigates T-04-21.
4. The doc explicitly says HSTS preload-list submission is deferred — mitigates T-04-20.
5. `astro.config.mjs` `site:` is the apex (D-07 verified).
6. `public/og-default.png` is 1200×630 cream-background brand-faithful (D-10 verified) OR a precise regeneration request is filed for Plan 05. SUMMARY contains the verbatim D-10 attestation line.
7. `CONTENT_EDITING.md` links to the new doc so the founder discovers it without being told.
</success_criteria>

<output>
After completion, create `.planning/phases/04-analytics-polish-launch/04-03-SUMMARY.md` per the template, with explicit notes on:
- The `file public/og-default.png` raw output (verify dimensions).
- The verbatim D-10 attestation line: `D-10 visual check: cream background = [yes|no], flower iconography = [yes|no], logo lockup/wordmark = [present|absent]` (executor fills the brackets based on actual visual inspection — this line is the D-10 gate per W-5; without it D-10 is not closed).
- The `grep` output proving `astro.config.mjs:8` is the apex.
- Final `wc -l SETUP-DNS.md`.
- A 1-line note flagging D-10 OK/FLAG status for Plan 05.
- Any deviations from the canonical text (should be minimal; record verbatim if any phrasing was changed).
</output>
</content>
</invoke>