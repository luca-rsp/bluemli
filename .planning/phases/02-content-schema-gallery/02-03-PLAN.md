---
phase: 02
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/design-skill/ProductSheet.jsx
  - scripts/check-brand-rules.sh
autonomous: true
requirements:
  - CNT-10
tags:
  - phase-1-cleanup
  - brand-check-fix
  - dead-code-removal
must_haves:
  truths:
    - "src/components/design-skill/ProductSheet.jsx no longer exists (CR-03 closed — Phase 1 dead-code-not-blocker now removed because D-09 picks photo-forward single column on the actual route, not a modal)"
    - "scripts/check-brand-rules.sh Rule 1 regex catches uppercase #FFF as well as lowercase #fff and 6-digit forms (CR-05 closed)"
    - "Rule 2 still passes against existing src/ + src/content/ after the cleanup (no regression)"
  artifacts:
    - path: scripts/check-brand-rules.sh
      provides: "Brand non-negotiable CI grep (FND-10) with fixed Rule 1 regex"
      contains: "#[fF]{3}"
  key_links:
    - from: "scripts/check-brand-rules.sh Rule 1"
      to: "every .astro/.jsx/.tsx/.ts/.css file under src/"
      via: "PCRE negative lookahead alternation"
      pattern: "#\\[fF\\]\\{3\\}.*#\\[fF\\]\\{6\\}"
---

<objective>
Close two Phase 1 carry-over quality-debt items flagged in `01-VERIFICATION.md` before Phase 2's content surfaces consume them:

1. **CR-03**: Delete `src/components/design-skill/ProductSheet.jsx`. Phase 1 verification classified it as "latent dead code, not a Phase 1 blocker" because no page imports it. D-09 then locked the detail-page composition as "photo-forward single column" — a real route, not a modal — so ProductSheet has no future role. Deleting now prevents future scavenger-hunt confusion where someone tries to wire it up.

2. **CR-05**: Fix `scripts/check-brand-rules.sh` Rule 1's regex so it catches `#FFF` (uppercase 3-digit hex) as well as `#fff` (lowercase 3-digit) and `#FFFFFF` (any-case 6-digit). The current regex `#fff(?![0-9a-fA-F])|#[fF]{6}` lowercases the 3-digit branch only. The fix is one PCRE character class change.

These two cleanups are independent of Plan 01/02 (no file overlap) so they execute in parallel during Wave 1.

Purpose: Phase 2 introduces `src/content/` — a new directory the brand-check Rule 1 scans (per Phase 1 D-09 scope). Tightening the regex now ensures future content-author whitespace mistakes (`color: #FFF`) fire CI. ProductSheet removal prevents Plan 04's executor from mistakenly wiring it up while looking for "an existing modal pattern."

Output: A cleaner `src/components/design-skill/` directory and a tighter brand-check regex with zero functional regressions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-brand-system/01-VERIFICATION.md
@.planning/phases/02-content-schema-gallery/02-CONTEXT.md
@.planning/phases/02-content-schema-gallery/02-RESEARCH.md

<interfaces>
<!-- Current brand-check Rule 1 regex (scripts/check-brand-rules.sh line 22) -->
Current pattern:  (bg-white|background:\s*white|#fff(?![0-9a-fA-F])|#[fF]{6})
  Catches: bg-white, background: white, #fff (lowercase only), #FFFFFF (any case 6-digit)
  Misses:  #FFF, #Fff, #fFF (mixed/upper case 3-digit)

Target pattern:   (bg-white|background:\s*white|#[fF]{3}(?![0-9a-fA-F])|#[fF]{6})
  Change: #fff → #[fF]{3} (character class accepts any case f/F, exactly 3 times)
  The negative lookahead (?![0-9a-fA-F]) whitelists #fff8 (cream tint with alpha — D-10 whitelisted value)

<!-- ProductSheet.jsx — confirmed unused by 01-VERIFICATION.md CR-03 rationale -->
File location: src/components/design-skill/ProductSheet.jsx (3069 bytes).
Imports of "ProductSheet" exist only in the file itself.
No page or layout imports it. Deletion is safe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete src/components/design-skill/ProductSheet.jsx</name>
  <files>src/components/design-skill/ProductSheet.jsx</files>
  <read_first>
    - src/components/design-skill/ProductSheet.jsx (confirm what's being deleted — review onClick patterns confirming it requires `client:load` to work, which D-09 explicitly rejects)
    - .planning/phases/01-foundations-brand-system/01-VERIFICATION.md (CR-03 rationale lines 41-50)
    - .planning/phases/02-content-schema-gallery/02-RESEARCH.md (Section 11 — CR-03 disposition: "Delete ProductSheet.jsx in Phase 2... D-09 explicitly picks photo-forward single column for the detail page — there is no modal")
    - .planning/phases/02-content-schema-gallery/02-CONTEXT.md (D-09 — photo-forward single column for /gallery/<slug>; not a modal)
  </read_first>
  <action>
**Step 1 — Verify no imports exist** (defensive check; CR-03 already confirmed but never trust stale doc):

Run `grep -rn "ProductSheet" src/pages/ src/layouts/ src/components/` and confirm the ONLY matches are inside the file itself (`src/components/design-skill/ProductSheet.jsx:` lines). If any `import` of `ProductSheet` appears in `src/pages/` or `src/layouts/`, STOP and report — do NOT delete. (This is a safety check; per 01-VERIFICATION.md it will not find imports.)

**Step 2 — Delete the file** with `rm src/components/design-skill/ProductSheet.jsx`.

**Step 3 — Verify no broken references** by running `pnpm exec astro check`. Confirm 0 errors. If a TypeScript error appears claiming a missing module `ProductSheet`, restore the file and investigate.

**What NOT to do:**
- Do NOT delete any other component in `src/components/design-skill/` — the other 10 components (About, AppointmentForm, BeadCluster, Button, Footer, GalleryGrid, Header, Hero, Mark, PopupStrip) are all actively used.
- Do NOT delete or modify `.claude/skills/studio-bluemli-design/ui_kits/website/ProductSheet.jsx` — that's the design-skill source of truth, kept as a deferred-modal reference (v1.x may re-introduce a modal).
- Do NOT add a comment somewhere referencing the deletion. The deletion is recorded in this plan's SUMMARY and in git history; that's sufficient.

**Why this is safe right now (Wave 1, parallel with Plans 01 and 02):**
- Plan 01 does NOT import ProductSheet (touches `src/content.config.ts`, content/, BaseLayout)
- Plan 02 does NOT import ProductSheet (touches scripts/, package.json, CI, gitignore)
- Plan 04 (Wave 2) WILL build the detail page but per D-09 uses photo-forward single column, NOT a modal — it doesn't need ProductSheet
- No risk of merge conflict — file ownership is exclusive to this plan.
  </action>
  <verify>
    <automated>test ! -f src/components/design-skill/ProductSheet.jsx &amp;&amp; { grep -rln "ProductSheet" src/pages/ src/layouts/ src/components/ 2>/dev/null | grep -v '^$' &amp;&amp; { echo "FAIL: residual references"; exit 1; } || true; } &amp;&amp; pnpm exec astro check 2>&amp;1 | tee /tmp/astro-check-cleanup.log &amp;&amp; grep -q "0 errors" /tmp/astro-check-cleanup.log</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/components/design-skill/ProductSheet.jsx` succeeds (file no longer exists)
    - No live imports anywhere in src/: `grep -rln "import.*ProductSheet" src/ 2>/dev/null | wc -l` returns `0`
    - All 10 sibling components still exist: `for f in About AppointmentForm BeadCluster Button Footer GalleryGrid Header Hero Mark PopupStrip; do test -f "src/components/design-skill/$f.jsx" || { echo "REGRESSION: $f.jsx missing"; exit 1; }; done` exits 0
    - The skill source ProductSheet (deferred reference) is still present: `test -f .claude/skills/studio-bluemli-design/ui_kits/website/ProductSheet.jsx`
    - `pnpm exec astro check` exits 0 with no missing-module errors
  </acceptance_criteria>
  <done>
    `src/components/design-skill/ProductSheet.jsx` deleted; no references in `src/`; all 10 sibling components intact; `astro check` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix CR-05 — uppercase #FFF in brand-check Rule 1 regex</name>
  <files>scripts/check-brand-rules.sh</files>
  <read_first>
    - scripts/check-brand-rules.sh (current state — Rule 1 at lines 14-32, regex on line 22)
    - .planning/phases/01-foundations-brand-system/01-VERIFICATION.md (CR-05 rationale lines 65-78: "the pattern is `#fff(?![0-9a-fA-F])|#[fF]{6}` — the 3-digit branch is lowercase-only; uppercase `#FFF` is not caught")
    - .planning/phases/02-content-schema-gallery/02-RESEARCH.md (Section 11 — CR-05 disposition: "Change `#fff(?![0-9a-fA-F])` to `#[fF]{3}(?![0-9a-fA-F])`")
    - .planning/phases/01-foundations-brand-system/01-CONTEXT.md (D-10 — `#fff8` is the one whitelisted near-white value; the negative lookahead `(?![0-9a-fA-F])` must continue to exclude it)
  </read_first>
  <action>
Edit `scripts/check-brand-rules.sh` line 22 (locate it via the anchor `bg-white|background:\s*white`). Change exactly ONE substring of the regex pattern:

**FROM** (current line 22):
`if grep -rnP '(bg-white|background:\s*white|#fff(?![0-9a-fA-F])|#[fF]{6})' \`

**TO**:
`if grep -rnP '(bg-white|background:\s*white|#[fF]{3}(?![0-9a-fA-F])|#[fF]{6})' \`

The single change: `#fff` becomes `#[fF]{3}`. Everything else on the line (the alternation operators, the negative lookahead, the 6-digit branch, the surrounding `grep -rnP` invocation, the `--include` flags on the following lines, the parenthesization) stays identical.

**Why this is correct:**
- `#[fF]{3}` matches `#FFF`, `#fff`, `#Fff`, `#fFF`, `#FfF`, `#FFf`, `#fFf`, `#ffF` (all 8 case combinations of 3 chars)
- The negative lookahead `(?![0-9a-fA-F])` still whitelists `#fff8` (the cream-tint-with-alpha value, D-10) by ensuring the 3 hex chars are NOT followed by another hex digit
- `#fff8a0`, `#FFF8`, `#FfF8` are all correctly whitelisted (4th char is hex, so lookahead fails the match)
- `#FFFEEE` continues to be caught by the existing `#[fF]{6}` branch (already case-insensitive)
- This regex does NOT affect Rules 2/3/4/5/7 — those are separate `grep` invocations

Do NOT add new rules. Do NOT modify Rule 2's exclusion list. Do NOT touch the commented-out Rule 7 (that's Plan 04's job — uncommenting Rule 7 AFTER sample-data.ts is deleted). Do NOT change the failure message at lines 27-31 (the message at line 30 reads "the only whitelisted near-white value is #fff8" which still applies).

**Self-test the regex against known inputs after editing** (do this before committing):

Cases that should MATCH (rule FAILS, i.e., grep finds them):
- `color: #FFF;` — must match
- `color: #fff;` — must match (existing behavior preserved)
- `color: #FFFFFF;` — must match via the 6-digit branch (existing behavior preserved)

Cases that should NOT match (rule PASSES, grep finds nothing):
- `color: #fff8;` — whitelisted alpha tint (existing behavior preserved)
- `color: #FFF8;` — whitelisted alpha tint (NEW behavior — previously the 3-digit branch wouldn't have matched at all here because of case, but now the lookahead correctly handles the 4-char case)
- `color: #fff8a0;` — whitelisted (4th char is hex, lookahead fails)

Validate by running `bash scripts/check-brand-rules.sh` on the current `src/` tree — it MUST exit 0 (no false positives introduced by the broader 3-digit class).
  </action>
  <verify>
    <automated>grep -F "#[fF]{3}(?![0-9a-fA-F])" scripts/check-brand-rules.sh &amp;&amp; ! grep -F "#fff(?![0-9a-fA-F])" scripts/check-brand-rules.sh &amp;&amp; printf 'color: #FFF;\n' | grep -P "#[fF]{3}(?![0-9a-fA-F])" &amp;&amp; ! { printf 'color: #fff8;\n' | grep -P "#[fF]{3}(?![0-9a-fA-F])"; } &amp;&amp; bash scripts/check-brand-rules.sh</automated>
  </verify>
  <acceptance_criteria>
    - Updated regex present: `grep -F "#[fF]{3}(?![0-9a-fA-F])" scripts/check-brand-rules.sh` succeeds
    - Old regex removed (no lowercase-only literal `#fff` in the Rule 1 branch): `grep -F "#fff(?![0-9a-fA-F])" scripts/check-brand-rules.sh ; test $? -eq 1` (grep returns 1 = not found)
    - 6-digit branch unchanged: `grep -F "#[fF]{6}" scripts/check-brand-rules.sh` succeeds
    - Self-test #FFF caught: `printf 'color: #FFF;\n' | grep -P "#[fF]{3}(?![0-9a-fA-F])"` succeeds
    - Self-test #fff caught: `printf 'color: #fff;\n' | grep -P "#[fF]{3}(?![0-9a-fA-F])"` succeeds
    - Self-test #fff8 whitelisted: `printf 'color: #fff8;\n' | grep -P "#[fF]{3}(?![0-9a-fA-F])" ; test $? -eq 1`
    - Self-test #FFF8 whitelisted: `printf 'color: #FFF8;\n' | grep -P "#[fF]{3}(?![0-9a-fA-F])" ; test $? -eq 1`
    - Full script still passes against current src/: `bash scripts/check-brand-rules.sh` exits 0
    - Rule 7 block remains COMMENTED OUT (Plan 04 owns uncommenting): `awk '/^# if grep -rEn .Sample Piece./,/^# fi$/' scripts/check-brand-rules.sh | wc -l` returns at least `5` (the commented block is still 5+ lines)
    - Line count of the script changed by ~0 (one-line in-place edit, no large additions): `wc -l < scripts/check-brand-rules.sh` returns a value within ±3 of the original `129` lines
  </acceptance_criteria>
  <done>
    Rule 1 regex updated to `#[fF]{3}` form; old `#fff` form removed; self-tests confirm uppercase #FFF is caught and #fff8/#FFF8 alpha tint is whitelisted; full `check-brand-rules.sh` still exits 0 against current source tree; Rule 7 remains commented (Plan 04's job to uncomment).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Brand-rule grep (Rule 1) → all .astro/.jsx/.tsx/.ts/.css under src/ | The regex decides which color values fail CI; a too-loose regex creates false positives, a too-tight regex misses violations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | Brand-non-negotiable bypass via uppercase hex (`#FFF` instead of `#fff`) | mitigate | Task 2's regex broadening (`#fff` → `#[fF]{3}`) closes this bypass while preserving the `#fff8` whitelist via the negative lookahead. Self-tests in acceptance criteria verify both halves. |
| T-02-08 | Repudiation / Confusion | Dead ProductSheet.jsx being mistakenly wired up as the detail-page modal | mitigate | Task 1 removes the file. The design-skill source-of-truth copy at `.claude/skills/studio-bluemli-design/ui_kits/website/ProductSheet.jsx` is preserved for v1.x reference but is not in any import path. |
</threat_model>

<verification>
After both tasks complete, run end-to-end validation:

```bash
# CR-03 closure
test ! -f src/components/design-skill/ProductSheet.jsx
grep -rln "ProductSheet" src/ 2>/dev/null | wc -l    # expect: 0

# CR-05 closure
grep -F "#[fF]{3}(?![0-9a-fA-F])" scripts/check-brand-rules.sh    # expect: present
grep -F "#fff(?![0-9a-fA-F])" scripts/check-brand-rules.sh        # expect: absent (exit 1)

# Self-tests
printf 'color: #FFF;\n'  | grep -P "#[fF]{3}(?![0-9a-fA-F])"      # expect: match
printf 'color: #fff8;\n' | grep -P "#[fF]{3}(?![0-9a-fA-F])"      # expect: no match

# Full integration — current tree must still pass
bash scripts/check-brand-rules.sh
pnpm exec astro check
```
</verification>

<success_criteria>
- ProductSheet.jsx deleted from `src/components/design-skill/`
- No stale references to ProductSheet anywhere in `src/`
- Rule 1 regex updated; 3-digit branch is case-insensitive via `[fF]{3}`
- `#fff8` whitelist preserved (negative lookahead still applies)
- `bash scripts/check-brand-rules.sh` exits 0 against current source tree (no new false positives)
- `pnpm exec astro check` exits 0
- Rule 7 remains commented out (Plan 04's responsibility to uncomment after Plan 04 deletes `src/sample-data.ts`)
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-schema-gallery/02-03-SUMMARY.md` documenting:
- CR-03 closure: ProductSheet.jsx deleted; rationale (D-09 photo-forward single column, not a modal)
- CR-05 closure: Rule 1 regex change with the exact before/after diff
- Confirmation that current src/ tree passes the updated brand-check
- Reminder note: Rule 7 enabling is Plan 04's responsibility (depends on sample-data.ts deletion first)
</output>
