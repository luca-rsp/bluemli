---
phase: 1
slug: foundations-brand-system
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-12
---

# Phase 1 — UI Design Contract: Foundations & Brand System

> Visual and interaction contract for Phase 1. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> This phase delivers a demo-loaded empty shell of all five pages at a `*.workers.dev` URL — no per-page feature UI.
> The design contract focuses on: global tokens, BaseLayout chrome, brand enforcement rules, and accessibility floor.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (Tailwind and shadcn are explicitly forbidden — see REQUIREMENTS.md Out of Scope) |
| Preset | not applicable |
| Component library | none — vanilla CSS custom properties via `src/styles/colors_and_type.css` (copied from design skill) |
| Icon library | Lucide — loaded as an ES module, not a CDN `<script>`. Stroke weight 1.5, size 20–24px, color `var(--color-fg)`. |
| Font — wordmark | Bagel Fat One (Google Fonts, self-hosted via Astro Fonts API) |
| Font — display headline | Caveat Brush (Google Fonts, self-hosted via Astro Fonts API) |
| Font — hand aside | Caveat (Google Fonts, self-hosted via Astro Fonts API — include only if a component references `--font-hand`; omit otherwise) |
| Font — body / UI | Nunito variable (Google Fonts, self-hosted via Astro Fonts API) |

**Source:** CONTEXT.md D-14, D-15, D-16; REQUIREMENTS.md FND-07; design skill README "Font substitutions."

**Swap path:** When the founder supplies a real hand-display WOFF2 file, it replaces Caveat Brush in the Astro Fonts config and drops into `public/fonts/`. A comment in `astro.config.mjs` marks the exact swap location.

---

## Spacing Scale

All tokens already exist in `colors_and_type.css` as `--space-*`. Use those variables; do not hard-code pixel values.

### Component spacing — Phase 1 In Use

These values belong to the standard 4pt grid and are the only spacing values used for component-internal layout in the BaseLayout shell.

| Token | Value | Design skill var | Usage |
|-------|-------|-----------------|-------|
| xs | 4px | `--space-1` | Icon gaps, inline padding, tight label spacing |
| sm | 8px | `--space-2` | Compact element spacing, gap between nav links |
| md | 16px | `--space-4` | Default element spacing, paragraph bottom margin |
| lg | 24px | `--space-5` | Component padding, card inner spacing |
| xl | 32px | `--space-6` | Mobile page margin (left/right), header padding |
| 2xl | 48px | `--space-7` | Footer top padding, section breaks |
| 3xl | 64px | `--space-8` | Hero section padding top |

Note: `--space-3` (12px) is present in `colors_and_type.css` but is not used in the Phase 1 BaseLayout shell. The BaseLayout uses `--space-2` (8px) or `--space-4` (16px) wherever a 12px temptation arises. It is listed in the inherited-but-unused table below.

### Layout-only tokens — inherited, not for component-internal spacing

| Token | Value | Design skill var | Justification |
|-------|-------|-----------------|---------------|
| 4xl | 96px | `--space-9` | Desktop page margin (left/right). Layout-scale token for section-level breathing room on wide viewports. Inherited from `colors_and_type.css`. Not used for component-internal padding. |
| 5xl | 128px | `--space-10` | Large section breathing room on wide viewports. Inherited from `colors_and_type.css`. Not used for component-internal padding. |

### Inherited but unused in Phase 1

| Token | Value | Design skill var | Status |
|-------|-------|-----------------|--------|
| md-sm | 12px | `--space-3` | Present in `colors_and_type.css`; not used in Phase 1 BaseLayout shell. May be activated in a later phase. |

**Exception — touch targets:** Every interactive element (nav links, buttons, skip-to-content) must have a minimum hit target of 44px × 44px regardless of visual size. Use padding to achieve this without changing visual dimensions.

**Source:** design skill `colors_and_type.css` `--space-*` tokens (pre-existing, LOCKED); design skill README "Generous margins" rule.

---

## Typography

All font-family and font-size tokens already exist in `colors_and_type.css`. Use CSS variables; do not hard-code values.

Phase 1 scope: only the tokens below render in the BaseLayout shell. Adding additional sizes or weights requires a new phase + spec update.

### Phase 1 — In Use

The primary body family (Nunito) is capped at **4 sizes** and **2 weights** in Phase 1. Display and wordmark families are separate typefaces and do not count against the Nunito budget.

| Role | Font family var | Size var | Size (px) | Weight | Line height var | Usage |
|------|----------------|----------|-----------|--------|----------------|-------|
| Body / UI text | `--font-body` (Nunito) | `--fs-sm` | 16px | 400 | `--lh-normal` (1.45) | Paragraphs, nav labels, UI text |
| Body strong / eyebrow | `--font-body` (Nunito) | `--fs-sm` | 16px | 700 | `--lh-normal` (1.45) | `<strong>`, nav active state, uppercase eyebrow labels |
| Footer caption / copyright | `--font-body` (Nunito) | `--fs-xs` | 14px | 400 | `--lh-normal` (1.45) | Copyright line, tagline, footer meta |
| h3 sub-section | `--font-body` (Nunito) | `--fs-xl` | 28px | 700 | `--lh-snug` (1.2) | Section sub-headings in sans |
| h2 section title | `--font-display` (Caveat Brush) | `--fs-3xl` | 48px | 400 | `--lh-snug` (1.2) | Section openers — display family, 1 weight, not counted against Nunito budget |
| h1 hero headline | `--font-display` (Caveat Brush) | `--fs-5xl` | 88px | 400 | `--lh-tight` (1.05) | Hero headline desktop. Mobile: clamp to `--fs-3xl` (48px) — responsive variant, not a new size |
| Wordmark | `--font-wordmark` (Bagel Fat One) | 28px inline | 28px | 400 | 1 | Header lockup and footer "Studio Bluemli" label only — separate family, 1 weight, not counted against Nunito budget |

**Nunito summary:** 4 sizes in use (14, 16, 28px — h3; note h1/h2 use the display family), 2 weights (400 regular, 700 bold). Within the ≤4 sizes / ≤2 weights checker budget.

**Casing rule:** Sentence case for all copy, including buttons and nav labels. Exception: NOPA geographic stamp stays uppercase. No all-caps for emphasis anywhere.

### Inherited from design-skill token file — not deployed in Phase 1

Tokens listed here are present in `colors_and_type.css` and may be activated in a later phase; they are out of scope for Phase 1 component styling.

| Role | Font family var | Size var | Size (px) | Weight | Notes |
|------|----------------|----------|-----------|--------|-------|
| Body large | `--font-body` (Nunito) | `--fs-md` | 18px | 400 | Hero subline, larger UI prose — activate in Phase 3 when per-page composition ships |
| Nunito medium | `--font-body` (Nunito) | — | — | 500 | Available in token file; not used in Phase 1 shell |
| Nunito semibold | `--font-body` (Nunito) | — | — | 600 | Available in token file; not used in Phase 1 shell |
| Nunito extrabold | `--font-body` (Nunito) | — | — | 800 | Available in token file; not used in Phase 1 shell |

**Source:** design skill `colors_and_type.css` `--fs-*`, `--lh-*`, `--font-*` tokens (pre-existing, LOCKED); design skill README "Typography in practice"; Hero.jsx, Header.jsx, Footer.jsx reference implementations.

---

## Color

All palette and semantic color tokens already exist in `colors_and_type.css`. Never invent new colors (brand non-negotiable #9).

### Surface split

| Role | Semantic var | Hex value | Split |
|------|-------------|-----------|-------|
| Dominant — page background | `--color-bg` → `--cream-100` | `#F5DCC7` | ~60% |
| Secondary — cards, elevated surfaces | `--color-surface-card` → `--cream-50` | `#FAEDE0` | ~30% |
| Soft well / sunken | `--color-bg-deep` → `--cream-200` | `#ECCDB3` | used sparingly |

### Foreground

| Role | Semantic var | Hex value |
|------|-------------|-----------|
| Default body text | `--color-fg` → `--indigo-500` | `#3E4E8B` |
| Headings, strong emphasis | `--color-fg-strong` → `--indigo-700` | `#2A3669` |
| Muted / captions | `--color-fg-muted` → `--ink-600` | `#6C5A47` |
| Hint / placeholders | `--color-fg-hint` → `--ink-400` | `#A48F77` |

### Accent (10% — reserved-for list is explicit)

Pick at most three accent colors per composition. Never all six at once.

| Accent | Semantic var | Hex | Reserved for |
|--------|-------------|-----|-------------|
| Hot coral | `--color-accent-hot` → `--coral-500` | `#D6553B` | Wordmark, primary CTA button fill, active nav state, link color, focus ring, h1/h2 color |
| Pop pink | `--color-accent-pop` → `--pink-500` | `#D87593` | "pop-up" badge/stamp, celebratory announcement badges |
| Leaf olive | `--color-accent-leaf` → `--olive-500` | `#6E7438` | Place names (NoPa, San Francisco), eyebrow labels, section-break horizontal rule |
| Sun mustard | `--color-accent-sun` → `--mustard-500` | `#E9B23F` | Sparkle accents, "new arrival" indicator — deferred to Phase 2+, do not use in Phase 1 shell |
| Iris lavender | `--color-accent-iris` → `--lavender-500` | `#9B85B5` | Available as a third accent if needed — use sparingly |

### Interactive states

| State | Token | Value |
|-------|-------|-------|
| Link default | `--color-link` | `var(--coral-500)` |
| Link hover | `--color-link-hover` | `var(--coral-700)` |
| Focus ring | `--color-focus-ring` | `var(--coral-500)` |
| Button hover (primary) | — | `var(--coral-700)` background |
| Button press | — | `scale(0.97)` transform |
| Card hover | — | lift 2–4px via `--shadow-md`; no border added |

### Hard prohibitions (CI-enforced)

These patterns are banned and will fail the CI brand-enforcement check (D-07, FND-10):

| Banned pattern | Allowed alternative |
|---------------|---------------------|
| `background: white`, `background: #fff`, `bg-white` | `var(--color-bg)` = `--cream-100` |
| `#fff` (bare 3-digit hex, no alpha) | `#fff8` (8-digit cream tint, whitelisted) or a named cream token |
| `gradient` (any CSS gradient) | Flat fills only |
| `backdrop-filter` | Not permitted — note: the existing Header.jsx uses `backdropFilter: 'blur(4px)'`. The Astro rewrite of Header MUST remove this and replace with a solid cream-100 background at 0.92 opacity using `background: rgba(245, 220, 199, 0.92)` without blur. |
| `border: 1px solid` | Use `--color-border-soft` (rgba) or `--shadow-xs` for soft separation |
| `flower`, `petal`, `floral`, `bloom`, `blossom` | Describe earrings as "beaded clusters" |

**Source:** CONTEXT.md D-07, D-10; REQUIREMENTS.md FND-10; design skill SKILL.md brand non-negotiables.

---

## BaseLayout Shell

This is the primary deliverable of Phase 1. Every page wraps in `BaseLayout.astro`.

### Head

```
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="color-scheme" content="light">             <!-- FND-13 -->
  <title>{title} — Studio Bluemli</title>
  <Font />                                               <!-- Astro Fonts API preload -->
  <style is:global>@import "../styles/colors_and_type.css";</style>
  <!-- Umami snippet: src/layouts/BaseLayout.astro, near </head> -->
  <!-- FND-13: skip-to-content link is the first element in <body> -->
</head>
```

### Skip-to-content (FND-13)

Position: first visible element on focus inside `<body>`, visually hidden until focused.

```
Copy: "Skip to main content"
Selector: #main-content on the <main> element
Style when focused: coral fill, cream text, top-left absolute, z-index: 100
```

### Header chrome

The Astro rewrite of `Header.jsx` must:
- Use `<header>` element with `role="banner"`
- Position: sticky, top 0, z-index 50
- Background: `rgba(245, 220, 199, 0.92)` — solid cream at 0.92 opacity. No `backdrop-filter` blur.
- Horizontal lockup: `mark.svg` (34×34px, `alt=""` — decorative, wordmark carries the text) + wordmark "Studio Bluemli" at 28px Bagel Fat One coral-500
- Nav links: Home, Gallery, Pop-ups, Say Hi — 16px Nunito 400; active: Nunito 700 coral-500; inactive: indigo-500
- All nav links use `<a href="/{slug}">` (real routes, not hash links)
- About page: not in the nav (per design skill Header.jsx — the nav is intentionally short)
- Mobile: nav links collapse to a hamburger. Lucide `menu` icon (24px, `--color-fg`). Drawer slides from right or stacks below the lockup. Minimum hit target 44px for the hamburger button.
- Hamburger button accessibility: `<button aria-label="Open navigation menu" aria-expanded="{state}">` where `aria-expanded` toggles `true`/`false` when the drawer opens and closes. The 44px minimum hit target is met via padding, not by enlarging the icon.
- Focus: every link and the hamburger must show the coral focus ring on `:focus-visible`

### Footer chrome

Match `Footer.jsx` reference:
- `<footer>` with `role="contentinfo"`
- Padding: 48px top, 56px bottom (matches `--space-7`)
- Centered stacked lockup: mark.svg (56×56px, `alt="Studio Bluemli"`) + wordmark "Studio Bluemli" at 28px
- Tagline: "hand-assembled earrings · made in NoPa, San Francisco" — 14px Nunito 400 indigo-500
- Links row: @studio_bluemli (Instagram), hi@studiobluemli.com (email) — coral-500; "NoPa, San Francisco" — olive-500 700
- Copyright: "© 2026 Studio Bluemli · all pairs one of a kind" — 14px (`--fs-xs`) Nunito 400 ink-600 letter-spacing 0.04em
- No `<hr>` / no `border-top`. Section separation via the `Mark.Dots` bead-cluster decoration (the 9-dot row from the reference — rendered as inline SVG or the BeadCluster component)

### Page container

```
<main id="main-content" style="max-width: 1200px; margin: 0 auto; padding: 0 var(--space-6);">
```

Desktop (≥ 768px): `padding: 0 var(--space-9)` (96px side margins).

---

## Breakpoints

Mobile-first. Phase 1 shell must be functional on all three widths.

| Name | Min-width | Notes |
|------|-----------|-------|
| mobile | 0 | Default — single column, 32px side padding |
| tablet | 640px | Nav wraps or hamburger activates below this |
| desktop | 768px | 96px side padding; nav shows inline |

There is no 1440px breakpoint in v1. `max-width: 1200px` on the container handles ultra-wide.

---

## Favicon Set

Generated once at scaffold time, committed to `public/`. No runtime generation.

| File | Source | Dimensions | `<link>` tag |
|------|--------|-----------|-------------|
| `public/favicon.ico` | Generated from `mark.svg` | 32×32 (multi-size .ico) | `<link rel="icon" href="/favicon.ico" sizes="any">` |
| `public/favicon-16.png` | Generated from `mark.svg` | 16×16 | `<link rel="icon" type="image/png" href="/favicon-16.png" sizes="16x16">` |
| `public/favicon-32.png` | Generated from `mark.svg` | 32×32 | `<link rel="icon" type="image/png" href="/favicon-32.png" sizes="32x32">` |
| `public/favicon.svg` | Copy of `assets/logo/mark.svg` | vector | `<link rel="icon" type="image/svg+xml" href="/favicon.svg">` |
| `public/apple-touch-icon.png` | Reuse `assets/logo/mark-favicon-180.png` as-is | 180×180 | `<link rel="apple-touch-icon" href="/apple-touch-icon.png">` |

Generation tool: planner's choice (sharp-cli or ImageMagick). Regen command documented in `public/favicon/README.md`.

**Source:** CONTEXT.md D-19, D-20; REQUIREMENTS.md FND-08.

---

## Font Loading

Self-hosted via Astro 6 Fonts API. No runtime Google Fonts request.

```ts
// astro.config.mjs
import { defineConfig } from "astro/config";
import { fonts } from "astro/assets";

export default defineConfig({
  experimental: { fonts: true },  // Astro 6.0 Fonts API
  // Font declarations:
  // - "Bagel Fat One" weights: [400]      → --font-wordmark
  // - "Caveat Brush" weights: [400]       → --font-display
  // - "Caveat" weights: [400]             → --font-hand (include only if used)
  // - "Nunito" weights: [400, 700]        → --font-body (Phase 1 in-use weights only)
  // font-display: swap on all faces (FND-07, Pitfall #10)
  // SWAP PATH COMMENT: Replace "Caveat Brush" with founder's custom WOFF2 here when provided.
});
```

`<Font />` component renders in `BaseLayout.astro` `<head>` — generates `@font-face` + `<link rel="preload">`.

`colors_and_type.css` original `@import url("https://fonts.googleapis.com/...")` line is REMOVED in the copied `src/styles/colors_and_type.css`. Fonts load only through Astro Fonts API.

Note: Nunito is declared with weights `[400, 700]` in Phase 1. Weights 500, 600, and 800 exist in the design-skill token file and may be added to the Fonts API declaration in a later phase without any other changes.

---

## Accessibility Floor

These rules apply from Phase 1 forward and cannot be removed.

| Rule | Implementation |
|------|---------------|
| Focus indicator | 2px solid `var(--color-focus-ring)` with 2px offset on every `:focus-visible` element. Never `outline: none` without a replacement. |
| Skip-to-content | First focusable element in `<body>`. Visually hidden until focused. Coral background, cream text. |
| Color scheme | `<meta name="color-scheme" content="light">` in every page `<head>`. |
| Contrast — body text | `--indigo-500` (#3E4E8B) on `--cream-100` (#F5DCC7): verified ≥ 4.5:1 ratio. |
| Contrast — coral on cream | `--coral-500` (#D6553B) on `--cream-100` (#F5DCC7): approximately 3.2:1 — meets AA for large text (18px+ or 14px+ bold) and UI components but NOT for normal body text. Coral is NEVER used for body prose. Coral is reserved for: wordmark, h1/h2 headings (≥48px display font), CTA button text (cream-50 on coral fill), link text. |
| Alt text — mark in header | `alt=""` — decorative; the wordmark "Studio Bluemli" next to it carries the label. |
| Alt text — mark in footer | `alt="Studio Bluemli"` |
| Alt text — placeholder photos | Phase 1 placeholder images must have descriptive alt text. Sample: `alt="Confetti earrings — colorful beaded cluster with mixed bead sizes"`. Never use blank alt on a content image. Never use words flower/petal/floral/bloom/blossom in alt text (CI rule). |
| Landmark roles | `<header role="banner">`, `<main id="main-content">`, `<footer role="contentinfo">`, `<nav aria-label="Site navigation">` |
| Hamburger button | `<button aria-label="Open navigation menu" aria-expanded="{state}">` — `aria-expanded` toggles `true`/`false` on drawer open/close. |

**Source:** REQUIREMENTS.md FND-13; design skill README "Interactive states," "Coral-on-cream" Pitfall #16, Pitfall #17.

---

## Demo-Loaded Page Shells

Each of the 5 pages renders the corresponding design-skill component with sample data. No real content collections in Phase 1.

| Route | File | Component(s) | Sample data |
|-------|------|-------------|------------|
| `/` | `src/pages/index.astro` | Hero, GalleryGrid (3 cards), PopupStrip (1 upcoming) | Inline or `src/sample-data.ts` — see CONTEXT.md D-02 |
| `/gallery` | `src/pages/gallery.astro` | GalleryGrid | 3–6 sample pieces (name: "Sample Piece A/B/C", price: 0) |
| `/popups` | `src/pages/popups.astro` | PopupStrip | 1 upcoming sample, 1 past sample |
| `/about` | `src/pages/about.astro` | About | Placeholder founder text, process-shot placeholder |
| `/say-hi` | `src/pages/say-hi.astro` | AppointmentForm | Static form shell (no POST endpoint until Phase 4) |

**Sample data marker requirement (D-03):** All sample prices must be `0`. All sample names must include "Sample". CI must fail the build after Phase 2 if any `"Sample Piece"` string survives in `src/content/`.

**No `client:` directives on any component.** All JSX renders server-side only.

---

## Copywriting Contract

Phase 1 is a shell, so copy is minimal. These are the only copy strings that must appear correctly in Phase 1.

| Element | Copy |
|---------|------|
| Page `<title>` pattern | `{Page Name} — Studio Bluemli` (e.g. "Gallery — Studio Bluemli") |
| Homepage `<title>` | `Studio Bluemli — handmade beaded earrings, NoPa San Francisco` |
| Header wordmark | `Studio Bluemli` (exact casing) |
| Nav: home | `home` (lowercase) |
| Nav: gallery | `gallery` (lowercase) |
| Nav: pop-ups | `pop-ups` (hyphenated, lowercase) |
| Nav: say hi | `say hi` (lowercase, two words) |
| Hero headline | `bright, beaded, one of a kind` (sentence case, display font) |
| Hero subline | `Hand-assembled earrings, made in NoPa. Sold at markets, pop-ups, and by appointment.` |
| Hero primary CTA | `see the gallery` (lowercase — brand voice; no period) |
| Hero secondary CTA | `next pop-up` (lowercase) |
| Footer tagline | `hand-assembled earrings · made in NoPa, San Francisco` |
| Footer copyright | `© 2026 Studio Bluemli · all pairs one of a kind` |
| Skip-to-content | `Skip to main content` |
| Hamburger open label | `Open navigation menu` (aria-label) |
| Sample piece name pattern | `Sample Piece A`, `Sample Piece B`, etc. |
| Sample piece price | `$0` |
| Empty state — no pop-ups | `no pop-ups on the calendar right now — DM me on Instagram` (Phase 1 shell can show a sample so this is deferred to Phase 3; documented here as the locked copy for later) |

**Prohibited vocabulary in all copy and alt text:** flower, petal, floral, bloom, blossom, artisanal, curated, luxury, "handcrafted with love", "under $100", emoji.

**Source:** CONTEXT.md D-01, D-03; design skill README "CONTENT FUNDAMENTALS"; Hero.jsx, Header.jsx, Footer.jsx.

---

## CI Brand Enforcement Rules

These are the patterns the GitHub Actions required status check must grep and fail on (CONTEXT.md D-07, REQUIREMENTS.md FND-10, FND-11).

| Rule | Grep pattern | Scope | Failure message |
|------|-------------|-------|----------------|
| Cream-only backgrounds | `bg-white\|background:\s*white\|#fff(?!8)` | `src/**/*.{css,astro,jsx,tsx,ts}` | "The site background must be cream (#F5DCC7), never white. Remove bg-white / #fff / background:white and use var(--color-bg)." |
| No flower vocabulary | `flower\|petal\|floral\|bloom\|blossom` | `src/**/*` and `src/content/**/*` | "The studio's earrings are beaded clusters, not flowers — pick a neutral word. See design skill README 'Vocabulary'." |
| No UI gradients | `gradient` | `src/**/*.{css,astro,jsx,tsx}` | "Gradients conflict with the Bluemli flat-color brand language. Use flat fills from the palette." |
| No backdrop-filter | `backdrop-filter` | `src/**/*.{css,astro,jsx,tsx}` | "backdrop-filter is frosted-glass SaaS — not Bluemli. Replace with a solid cream background at opacity." |
| No 1px borders | `border:\s*1px` | `src/**/*.{css,astro,jsx,tsx}` | "Hard 1px borders are not Bluemli. Use --color-border-soft (rgba), --shadow-xs, or no border at all." |
| Lowercase filenames | any uppercase letter in `src/pages/**/*` filenames | `src/pages/` file listing | "All src/pages/ filenames must be lowercase-only. macOS is case-insensitive; Linux is not — mixed case causes 404s in production." |
| Sample data survives Phase 2+ | `"Sample Piece"` | `src/content/**/*` | "Remove sample-data markers before merging. Phase 2 ships real content." (Only active after Phase 2 ships.) |

**Exclusions from grep scope:** `dist/`, `node_modules/`, `.planning/`, `.claude/`, `CONTENT_EDITING.md`, `public/favicon/README.md`.

**Allowed exception:** `#fff8` (8-character hex with alpha channel) is the one whitelisted near-white value. The pattern `#fff(?!8)` excludes it via negative lookahead.

---

## Registry Safety

No shadcn, no third-party component registries. Not applicable to this project.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | not applicable | not applicable |

---

## Source Traceability

| Section | Decisions Pre-Populated From |
|---------|------------------------------|
| Design system | CONTEXT.md D-14–D-17; REQUIREMENTS.md FND-06, FND-07; CLAUDE.md styling approach |
| Spacing scale | design skill `colors_and_type.css` `--space-*` (LOCKED) |
| Typography | design skill `colors_and_type.css` `--font-*`, `--fs-*`, `--lh-*` (LOCKED); Hero.jsx, Header.jsx reference |
| Color | design skill `colors_and_type.css` palette + semantic tokens (LOCKED); SKILL.md brand non-negotiables |
| BaseLayout shell | CONTEXT.md D-01, D-04, D-18; REQUIREMENTS.md FND-05; Header.jsx, Footer.jsx reference |
| Font loading | CONTEXT.md D-14–D-17; REQUIREMENTS.md FND-07 |
| Favicon set | CONTEXT.md D-19, D-20; REQUIREMENTS.md FND-08 |
| Accessibility floor | REQUIREMENTS.md FND-13; design skill README Pitfalls #16, #17 |
| CI enforcement | CONTEXT.md D-07–D-11; REQUIREMENTS.md FND-10, FND-11 |
| Copywriting | design skill README "CONTENT FUNDAMENTALS"; Hero.jsx, Header.jsx, Footer.jsx |
| No questions asked | Stack (CLAUDE.md); fonts (CONTEXT.md D-14–D-16); all brand tokens (LOCKED by design skill) |

Zero design questions were asked of the user. All decisions pre-populated from upstream artifacts.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
