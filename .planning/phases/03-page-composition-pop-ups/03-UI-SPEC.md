---
phase: 3
slug: page-composition-pop-ups
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-13
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for all Phase 3 surfaces: landing page (Hero + mini-callout + featured grid), `/popups` (full editorial list), `/about` (portrait + photo strip), `/say-hi` (IG-link page). Also covers shared infrastructure: `SEO.astro`, `robots.txt`, sitemap, the Bagel Fat One → Caveat Brush wordmark swap (D-24), and the `NoPa → NOPA` casing fix (D-25). Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> Every value below is LOCKED upstream — by CLAUDE.md, the studio-bluemli-design skill, the sketch-findings-bluemli skill, and `03-CONTEXT.md`. The executor reproduces these tokens and patterns exactly. No invention.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (shadcn explicitly rejected — see "What NOT to Use" in CLAUDE.md; `colors_and_type.css` is the single source of truth) |
| Preset | not applicable |
| Component library | none — React 19 JSX from the design skill, server-rendered by Astro 6.2 (no `client:` directives, zero client JS shipped) |
| Icon library | none — arrow glyphs (`→`, `←`) are literal Unicode characters in markup |
| Font | **Caveat Brush** (`--font-display`) for all display headlines (Hero, `/say-hi`, `/popups` PopupStrip, `/about` name). **Caveat** (`--font-hand`) for handwritten touches (landing mini-callout eyebrow, `/about` signature). **Nunito** (`--font-body`) for all UI, body, and eyebrow type. **`--font-wordmark`** changes from Bagel Fat One → `var(--font-display-loaded), 'Caveat Brush', cursive` (D-24). All self-hosted WOFF2 with `font-display: swap` (FND-07). |
| Token source | `src/styles/colors_and_type.css` — the single source of truth. Never hardcode hex. Never invent values. |
| Styling approach | Vanilla CSS via Astro's auto-scoped `<style>` blocks in `.astro` files; global tokens imported once in `BaseLayout.astro`. No Tailwind, no CSS-in-JS, no BEM (CLAUDE.md styling approach). |

---

## Spacing Scale

The token table below is the complete spacing system from `colors_and_type.css`. Phase 3 uses the same tokens as Phase 2 — the scale is not extended or modified. Token names are the canonical references for all measurements in this document.

| Token | Value | Usage in Phase 3 |
|-------|-------|------------------|
| `--space-1` | 4px | Tight inline gaps |
| `--space-2` | 8px | Signature gap; arrow hover slide amount; small inline spacing |
| `--space-3` | 12px | Mini-callout eyebrow margin-bottom; button vertical padding; CTA stack gap |
| `--space-4` | 16px | Mini-callout time margin-bottom; photo strip gap; meta row gap |
| `--space-5` | 24px | Horizontal page padding (all surfaces); mini-callout horizontal padding; lists horizontal padding |
| `--space-6` | 32px | Mini-callout vertical padding; IG button horizontal padding |
| `--space-7` | 48px | `/popups` section eyebrow margin-top; `/say-hi` sub → button spacing; CTA stack → back-link spacing (from Phase 2) |
| `--space-8` | 64px | `/about` text-block top padding; photo strip margin-top; photo strip bottom padding |
| `--space-9` | 96px | `/popups` lists bottom padding; `/popups` empty-state margin above/below; `/say-hi` stage top padding |

Exceptions: none. The 8-point grid is the brand non-negotiable. `--space-3` (12px) is a documented 4pt half-step on the same grid, declared in the token file.

---

## Typography

The brand has an 11-size system (`--fs-xxs` → `--fs-6xl`) plus raw-px Caveat Brush display sizes. Phase 3 uses **9 distinct sizes** across all surfaces. This follows the same pattern documented in Phase 2's non-blocking flag — the token file is the locked source, not Phase-3 invention. All choices are explicitly role-mapped below.

### Font families in Phase 3 (and where each is used)

| Variable | Family | Surfaces / Roles |
|----------|--------|-----------------|
| `--font-display` | Caveat Brush | `/say-hi` h1 (`clamp(56px,9vw,96px)`); `/about` headline (56px); `/popups` PopupStrip "pop-up" (80px) and "at venue" (32px) |
| `--font-hand` | Caveat | Landing mini-callout eyebrow (28px, -1.5° rotation); `/about` signature (22px) |
| `--font-body` | Nunito | All other type: eyebrows, when-line, body copy, buttons, fallback links, section lists |
| `--font-wordmark` | → Caveat Brush (D-24) | "Studio Bluemli" wordmark in Header + Footer; updated via `--font-wordmark` cascade change in `colors_and_type.css` (D-24); no new download, aliases the already-loaded Caveat Brush |

### Per-surface type tables

#### Landing page (`/`)

| Role | Token / px | Font | Weight | Line height | Color token |
|------|-----------|------|--------|-------------|-------------|
| Mini-callout eyebrow | `--fs-xl` 28px | `--font-hand` (Caveat) | 400 | 1 | `--color-accent-leaf` (olive-500), rotate -1.5° |
| Mini-callout when-line | `--fs-xl` 28px | `--font-body` (Nunito) | 800 | 1.15 | `--color-fg-strong`; venue span → `--coral-500` |
| Mini-callout time | `--fs-sm` 16px | `--font-body` (Nunito) | 400 | 1.45 | `--color-fg-muted` |
| "See all" link (conditional) | `--fs-xs` 14px | `--font-body` (Nunito) | 700 | 1 | `--coral-500`; hover → `--coral-700` |

Note: The landing Hero (`Hero.jsx`) retains its existing type treatment (D-01 — no changes except D-25 NOPA casing fix). The featured-pieces grid (`GalleryGrid.jsx`) retains Phase 2's type contract. Both are out of scope for redefinition here.

#### `/popups` page

| Role | Token / px | Font | Weight | Line height | Color token |
|------|-----------|------|--------|-------------|-------------|
| PopupStrip "pop-up" headline | 80px raw | `--font-display` (Caveat Brush) | 400 | `--lh-tight` 1.05 | `--coral-500` (existing PopupStrip behavior) |
| PopupStrip "at venue" | 32px raw | `--font-display` (Caveat Brush) | 400 | `--lh-snug` 1.2 | `--color-fg-strong` |
| PopupStrip date-time | `--fs-md` 18px | `--font-body` (Nunito) | 700 | `--lh-normal` 1.45 | `--color-fg-strong` |
| Section eyebrow ("ALSO COMING UP" / "PAST POP-UPS") | `--fs-xxs` 12px | `--font-body` (Nunito) | 800, ALL CAPS, `--ls-caps` | `--lh-snug` | `--color-accent-leaf` (olive-500) |
| ALSO COMING UP row | `--fs-md` 18px | `--font-body` (Nunito) | 700 | `--lh-normal` | `--color-fg-strong`; time span → `--color-fg-muted` weight 400 |
| PAST row | `--fs-sm` 14px | `--font-body` (Nunito) | 400 | `--lh-normal` | `--color-fg-muted`; venue span → `--color-fg` |
| Empty-state eyebrow | `--fs-xxs` 12px | `--font-body` (Nunito) | 800, ALL CAPS, `--ls-caps` | — | `--color-accent-leaf` |
| Empty-state body | `--fs-lg` 22px | `--font-body` (Nunito) | 400 | `--lh-loose` 1.7 | `--color-fg-strong` |

#### `/about` page

| Role | Token / px | Font | Weight | Line height | Color token |
|------|-----------|------|--------|-------------|-------------|
| Eyebrow ("ABOUT THE STUDIO") | `--fs-xxs` 12px | `--font-body` (Nunito) | 800, ALL CAPS, `--ls-caps` | — | `--color-accent-leaf` (olive-500) |
| Headline | 56px raw | `--font-display` (Caveat Brush) | 400 | 1.05 | `--coral-500` |
| Body paragraphs | `--fs-md` 18px | `--font-body` (Nunito) | 400 | `--lh-loose` 1.7 | `--color-fg` (indigo-500) |
| Signature "made with love from NOPA ♡" | `--fs-lg` 22px | `--font-hand` (Caveat) | 400 | 1 | `--color-fg-muted`; ♡ → `--coral-500` |

#### `/say-hi` page

| Role | Token / px | Font | Weight | Line height | Color token |
|------|-----------|------|--------|-------------|-------------|
| Headline `say hi` | `clamp(56px, 9vw, 96px)` | `--font-display` (Caveat Brush) | 400 | 1 | `--coral-500` |
| Sub-tagline `let's talk earrings` | `--fs-xl` 28px | `--font-hand` (Caveat) | 400 | 1 | `--color-fg-strong` |
| IG button label | `--fs-md` 18px | `--font-body` (Nunito) | 800 | 1 | `--color-fg-on-coral` |
| Mailto fallback | `--fs-sm` 16px | `--font-body` (Nunito) | 400 | `--lh-normal` | `--color-fg-muted`; email link → `--coral-500` |

#### Shared infrastructure (`SEO.astro`, `robots.txt`, sitemap)

No visible type — these surfaces emit `<meta>` tags, machine-readable text, and XML. No type contract required.

---

## Color

Bluemli's 60/30/10 split and "pick three accent colors per composition, never all five" rule (brand non-negotiable #8) carry forward unchanged from Phase 2. Phase 3 introduces olive (`--color-accent-leaf`) more prominently in eyebrows alongside coral. Per composition: coral + indigo + olive across the Phase 3 surfaces (lavender appears only on sold-status badges inherited from Phase 2 gallery cards, not as a new Phase-3 accent).

### 60 / 30 / 10 split (Phase 3 surfaces)

| Role | Token | Hex | Usage on Phase 3 surfaces |
|------|-------|-----|--------------------------|
| Dominant (60%) — page surface | `--cream-100` via `--color-bg` | #F5DCC7 | Page background on all surfaces. Cream, never white (brand non-negotiable #1 + Phase 1 CI grep: `bg-white`, `#fff`). |
| Secondary (30%) — text + photography | `--indigo-500` via `--color-fg` + `--indigo-700` via `--color-fg-strong`, plus product photography | #3E4E8B / #2A3669 | Body copy, headings, section text. Photos carry the page (non-negotiable #10). |
| Accent (10%) — coral headline + CTA + links | `--coral-500` / `--coral-700` (hover/press) | #D6553B / #A93C26 | Reserved-for list below — explicit and exhaustive. |

### Accent (`--coral-500`) reserved for — explicit, exhaustive list

No element other than the following may use `--coral-500` or `--coral-700` on Phase 3 surfaces:

1. **`/say-hi` headline** (`say hi`) — `--coral-500`.
2. **`/about` display headline** (Caveat Brush, 56px) — `--coral-500`.
3. **`/popups` PopupStrip "pop-up" headline** (80px) — `--coral-500` (existing `PopupStrip.jsx` behavior, retained).
4. **Landing mini-callout venue name** — `--coral-500` span inside the `.when` line.
5. **"See all upcoming pop-ups →" link** on landing (conditional, 2+ upcoming) — `--coral-500` rest, `--coral-700` hover.
6. **IG DM button background** on `/say-hi` — `--coral-500` rest, `--coral-700` hover.
7. **Mailto email address link** on `/say-hi` — `--coral-500`, underlined.
8. **`@studiobluemli` link** in `/popups` empty state — `--coral-500`, underlined.
9. **♡ glyph** in `/about` signature — `--coral-500`.
10. **Focus rings** (`:focus-visible` on every interactive element across all Phase 3 surfaces) — `--color-focus-ring` (= `--coral-500`).

### Supporting surface tokens (Phase 3)

| Role | Token | Hex | Usage |
|------|-------|-----|-------|
| Eyebrow text (section labels) | `--color-accent-leaf` (= `--olive-500`) | #6E7438 | "ALSO COMING UP", "PAST POP-UPS", "ABOUT THE STUDIO", `/popups` empty-state eyebrow, mini-callout handwritten eyebrow |
| Photo fallback (loading) | `--cream-200` | #ECCDB3 | `background-color` on about-strip `<img>` cells while loading |
| About photo strip corner | `--radius-sm` | 8px | `border-radius` on each 1:1 photo cell |
| IG button corner | `--radius-pill` | 999px | `border-radius` on `/say-hi` pill button |
| ALSO COMING UP rule lines | `--color-border-soft` | (from token file) | `border-top` on each `.also-row`; `border-bottom` on `:last-of-type` |
| Separator dots in list rows | `--ink-400` | (from token file) | `·` glyphs between date / venue / time in ALSO COMING UP + PAST rows |
| Muted text for time / past rows | `--color-fg-muted` | (from token file) | Time portion of ALSO COMING UP rows; all PAST row text except venue |
| Venue emphasis in PAST rows | `--color-fg` | #3E4E8B | Venue substring upgraded from muted, for scannability |

### Destructive

| Role | Value |
|------|-------|
| Destructive | not applicable — Phase 3 has no destructive user actions. The only mutation is the founder editing markdown files; no in-browser destructive confirm flow exists on these surfaces. |

### Brand non-negotiables enforced by CI (relevant to Phase 3)

- No `bg-white`, `background: white`, `#fff` — page is cream (FND-10, Rule 1; Phase 1 CI grep with `#fff8` exception).
- No `gradient`, `backdrop-filter`, `border: 1px` (FND-10, Rules 4–6). Phase 3 uses solid fills and `border-radius` only.
- No `flower|petal|floral|bloom|blossom` anywhere in `src/` or `src/content/` (FND-10, Rule 2; PAG-09). About-page body copy, alt text, and all new Phase 3 copy must clear this grep.
- No uppercase filenames under `src/pages/` (FND-11, Phase 1 CI grep).
- `NoPa` → `NOPA` in all user-facing site copy (D-25). Applies to: Hero.jsx eyebrow + sub-tagline, About.jsx copy, Footer.jsx tagline, `src/content/site/config.yaml`.

---

## Copywriting Contract

Voice rules (locked by design-skill README + 03-CONTEXT.md carrying-forward): warm, casual, founder-first, sentence-case, friendly parentheticals, no emoji. The only decorative glyph permitted is `♡`/`♥` in coral. Never "under $100". Never flower vocabulary in alt text or copy.

### CTAs

| Surface | Element | Copy | Target |
|---------|---------|------|--------|
| `/say-hi` | Primary button (pill, coral) | `DM me on Instagram →` | `https://ig.me/m/studiobluemli` |
| `/say-hi` | Mailto fallback (small text below button) | `or email hi@studiobluemli.com` | `mailto:hi@studiobluemli.com` |
| Landing mini-callout | "See all" link (only when 2+ upcoming) | `see all upcoming pop-ups →` | `/popups` |
| Landing mini-callout | No embedded CTA (D-05) | — | — |

### Empty states

| Surface | Trigger | Heading / eyebrow | Body copy |
|---------|---------|-------------------|-----------|
| Landing mini-callout | Zero upcoming pop-ups | (none — entire section omitted, D-03) | (none) |
| `/popups` | Zero upcoming AND zero past | Eyebrow: `Pop-ups` (caps, olive-500) — NO hand-display headline (D-08) | `No pop-ups on the calendar yet —` / `follow @studiobluemli for the next one.` — `@studiobluemli` is a coral underlined link to the IG profile |

### Error states

Phase 3 surfaces are prerendered (`prerender = true`). There are no runtime fetches, no client-side state, no form submissions in Phase 3. The only runtime "error" path on these pages is a `robots.txt.ts` endpoint that branches on environment — this returns machine-readable text, not a user-facing error. No visitor-facing error UI is in scope for Phase 3.

### Destructive confirmation

| Action | Confirmation copy |
|--------|-------------------|
| not applicable | No destructive actions in Phase 3 (no form, no delete, no submit). |

### Landing mini-callout format strings

| Element | Format | Example |
|---------|--------|---------|
| When-line | `<Weekday>, <Month> <Day> — <Venue>` | `Saturday, June 7 — Heath Ceramics` |
| Time-line | `<start_time>–<end_time>` (en-dash, not hyphen) | `11am–4pm` |

Em-dash separator (not `·`) in the when-line — matches the handwritten eyebrow's looser register (sketch-findings reference, open question resolved: em-dash).

### `/popups` list format strings

| List | Format | Example |
|------|--------|---------|
| ALSO COMING UP row | `<Weekday>, <Month> <Day> · <Venue> · <time>` | `Sunday, July 6 · Heath Ceramics · 11am–4pm` |
| PAST row | `<Weekday>, <Month> <Day> · <Venue>, <City>` | `Saturday, April 12 · Heath Ceramics, San Francisco` |

City fallback: `San Francisco` if not present in frontmatter.

### `/about` copy (D-13 — Claude-drafted placeholder, founder edits later)

The executor writes a first-person portrait in the design-skill voice:
- Eyebrow: `about the studio` (lowercase in markup; CSS `text-transform: uppercase`).
- Caveat Brush headline: 1–4 words, brand-voiced, coral. Example: `made by hand`.
- Body: 2–3 paragraphs, 18px Nunito, sentence-case, friendly parentheticals, NOPA in caps, no emoji, no flower vocabulary. Covers: origin story, the earring-making process, NOPA studio.
- Signature: `made with love from NOPA ♡` — exactly this phrase (D-16). ♡ in coral.

### Alt text contract (about photo strip)

Pull `alt` from the piece's `name` field (same pattern as Phase 2 gallery — `alt={piece.data.name}`). Flower vocabulary forbidden. The CI grep enforces this on all `src/` files.

### Photo selection for `/about` strip (D-14, 1–3 pieces)

Claude picks 3 pieces from the `gallery` collection during execution based on visual color balance — aim for coral / pink / cobalt or olive spread. No founder face in any photo (ROADMAP lock). If fewer than 3 gallery pieces are published, render what's available — do not show empty cream cells.

---

## Layout & Composition Contract (LOCKED from sketch-findings-bluemli)

All layouts are validated and prescriptive. Executors reproduce exactly. See `.claude/skills/sketch-findings-bluemli/references/pop-ups-across-surfaces.md` and `.../references/static-narrative-pages.md` for the canonical CSS + HTML structures — those files take precedence over summaries here.

### Landing page (`/`) — mini-callout (Sketch 003-C winner)

- **Condition:** render only when `upcomingPopups.length >= 1`. When zero upcoming, omit the entire section (D-03).
- **Container:** `.popup-callout`, `max-width: 640px`, `margin: 0 auto`, `padding: var(--space-6) var(--space-5)`, `text-align: center`. No band, no card background, no border.
- **Vertical order:** `span.hand-eyebrow` (Caveat, 28px, olive, -1.5° rotation, `display: inline-block`) → `p.when` (Nunito 800, 28px, venue in coral) → `p.time` (Nunito 400, 16px, muted) → `a.see-all` (only when `upcomingPopups.length >= 1`, conditionally rendered, Nunito 700, 14px, coral, right-arrow with +2px hover slide).
- **Position on landing:** between Hero CTAs / bead cluster and the featured-pieces grid.
- **Featured-pieces grid:** 3 most-recent `featured: true` pieces (D-04). Falls back to newest 3 regardless of featured flag if zero pieces are `featured: true`. Sorted by `published_at` desc.

### `/popups` page (Sketch 004-A winner)

- **Column:** centered, `max-width: 720px`, `padding: 0 var(--space-5) var(--space-9)`.
- **Layer 1 — soonest upcoming:** full `PopupStrip` JSX. D-09: "book by appointment" CTA block is **deleted** from `PopupStrip.jsx`. No replacement CTA. PopupStrip is: eyebrow → "pop-up" Caveat Brush 80px → "at venue" Caveat Brush 32px → date-time Nunito 18px bold.
- **Layer 2 — ALSO COMING UP (only when `upcomingPopups.length >= 2`):** section eyebrow (Nunito 800, 12px, ALL CAPS, olive); then rows, each `border-top: 1px solid var(--color-border-soft)`; last row has `border-bottom` too; format: `weekday date · venue · time`.
- **Layer 3 — PAST (only when `past.length >= 1`):** section eyebrow same style; then compact text rows, no rule lines; Nunito 400, 14px, muted; format: `weekday date · venue, city`.
- **Empty state (D-08):** When both upcoming and past arrays are empty — eyebrow "Pop-ups" (12px, olive, caps) + two-line body (Nunito 22px, `--lh-loose`) + `@studiobluemli` as coral underlined IG link. No Caveat Brush headline, no big visual.
- **Photos in PAST:** intentionally NOT rendered (D-07). Schema's optional `photos: image()` array is ignored in v1.
- **Per-popup detail routes:** do not exist (D-10). No `[slug].astro` for popups.

### `/about` page (Sketch 005-B winner)

- **Text block:** `.about-text`, `max-width: 720px`, `margin: 0 auto`, `padding: var(--space-8) var(--space-5) 0`. Single column, mobile-first.
- **Vertical order of text block:** eyebrow (`--fs-xxs`, olive, caps) → Caveat Brush headline (56px, coral) → 1–2 body paragraphs (Nunito 18px, `--lh-loose`, `max-width: 560px` centered within the 720px column, `text-wrap: pretty`) → signature (Caveat 22px, muted, inline-flex, ♡ in coral).
- **Photo strip:** `.about-photo-strip`, `max-width: 720px`, `margin: var(--space-8) auto 0`, `padding: 0 var(--space-5) var(--space-8)`. CSS grid, `grid-template-columns: repeat(3, 1fr)`, `gap: var(--space-4)`. Each cell: `aspect-ratio: 1 / 1`, `border-radius: var(--radius-sm)`, `background-color: var(--cream-200)`, `object-fit: cover`. Same 720px column as text — **not full-bleed**.
- **Fewer-than-3 fallback:** `grid-template-columns: repeat(auto-fit, minmax(0, 1fr))` — renders 1 or 2 photos naturally, no empty cream cells.
- **No press / "as featured in" section** (D-17 — anti-feature; REQUIREMENTS.md Out of Scope).

### `/say-hi` page (Sketch 006-A refined, no eyebrow)

- **Container:** `.say-hi-stage`, `max-width: 720px`, `min-height: 540px`, `margin: 0 auto`, `padding: var(--space-9) var(--space-5) var(--space-7)`. `display: flex; flex-direction: column; align-items: center; justify-content: center; text-align: center`. The `min-height` is critical — prevents awkward mid-viewport placement on tall viewports.
- **No eyebrow** — headline leads directly (D-18's no-form decision; eyebrow would duplicate "say hi").
- **Vertical order:** `h1` (Caveat Brush, `clamp(56px,9vw,96px)`, coral) → `p.sub` (Caveat 28px, `--color-fg-strong`, margin-bottom `--space-7`) → `a.ig-button` (coral pill, Nunito 800, 18px, `DM me on Instagram →`) → `p.mailto` (Nunito 400, 16px, muted, `or email hi@studiobluemli.com`).
- **IG button:** `background: var(--coral-500)`, `border-radius: var(--radius-pill)`, `padding: var(--space-3) var(--space-6)`. Hover: `background: var(--coral-700)`. Arrow `→` slides +3px on hover (`transition: transform .15s`).
- **No `<form>`**, no fields, no submit button, no Turnstile, no Worker endpoint (D-18). `AppointmentForm.jsx` import dropped; file remains in `src/components/design-skill/` (D-21).

### Shared `SEO.astro` component (D-26)

- Emits: `<title>`, `<meta name="description">`, `og:title`, `og:description`, `og:image`, `og:url`, `twitter:card="summary_large_image"`, `<link rel="canonical">`.
- Defaults: from `getEntry('site', 'default').data` (`og_title`, `og_description`). Per-page overrides via props.
- Canonical: always apex `https://studiobluemli.com` (via `Astro.site`), even on preview deploys (D-26; previews are not indexed by search engines for non-custom-domain sites; D-29's robots.txt closes the remaining gap).
- Default `og:image` (D-27 planner recommendation, path 3): static logo-lockup PNG at 1200×630 on cream background — executor creates `public/og-default.png`. Per-page overrides: `/gallery/<slug>` continues to pass the piece's 800w hero as `og:image` (env-aware base-URL resolution pattern lifted from Phase 2).
- Injected via `BaseLayout.astro`'s `<slot name="head" />` on all pages.

### `robots.txt` (D-29)

- Implemented as `src/pages/robots.txt.ts` (Astro endpoint, `export const prerender = false`).
- Production (`Astro.site === 'https://studiobluemli.com'`): `User-agent: *\nAllow: /\nSitemap: https://studiobluemli.com/sitemap-index.xml`.
- Preview / workers.dev hosts: `User-agent: *\nDisallow: /`.
- No visible UI — machine-readable only.

### Wordmark font swap (D-24)

Three targeted edits, zero new downloads:
- `src/styles/colors_and_type.css`: `--font-wordmark` cascade changed from `'Bagel Fat One', 'Pacifico', 'Lobster', cursive` → `var(--font-display-loaded), 'Caveat Brush', cursive`.
- `src/layouts/BaseLayout.astro`: remove the `<Font cssVariable="--font-wordmark-loaded" preload />` tag.
- `astro.config.mjs`: remove the Bagel Fat One Fonts API entry.
- Header.jsx picks up the cascade automatically via `var(--font-wordmark)`.

### `NoPa → NOPA` casing fix (D-25)

Apply to user-facing copy in: `Hero.jsx` eyebrow + sub-tagline, `About.jsx` body copy, `Footer.jsx` tagline, `src/content/site/config.yaml` (tagline, footer_text, og_description). Do NOT touch: `.planning/` documents, `.claude/skills/studio-bluemli-design/`, code comments, `CLAUDE.md` planning prose, commit messages.

---

## Focus / Accessibility Contract (carries forward FND-13)

- Every interactive element on Phase 3 surfaces has a `:focus-visible` outline using `--color-focus-ring` (= `--coral-500`), 2px solid, `outline-offset: 3px` (buttons) or `4px` (links).
- No `outline: none` without a paired `:focus-visible` style (Phase 1 Pitfall #17).
- All about-strip photos have `alt={piece.data.name}`.
- Tab order matches reading order on all Phase 3 pages; no `tabindex` overrides needed.
- The skip-to-content link from Phase 1's BaseLayout is inherited — no changes needed.

---

## Interaction States

| Element | Rest | Hover | Focus-visible | Active / Pressed |
|---------|------|-------|---------------|------------------|
| Mini-callout "see all" link | `--coral-500`, no underline | `--coral-700`; arrow slides +2px | 2px solid `--color-focus-ring`, offset 4px | (link navigates) |
| `/say-hi` IG button | `background: --coral-500` | `background: --coral-700`; arrow slides +3px | 2px solid `--color-focus-ring`, offset 3px | (link navigates) |
| `/say-hi` mailto link | `--coral-500`, underlined, `text-underline-offset: 3px` | `--coral-700` | 2px solid `--color-focus-ring`, offset 4px | (link navigates) |
| `/popups` empty-state `@studiobluemli` | `--coral-500`, underlined | `--coral-700` | 2px solid `--color-focus-ring`, offset 4px | (link navigates) |

No animations, no transitions beyond the existing global `a { transition: color var(--dur-fast) var(--ease-soft) }` declared in `colors_and_type.css`. No reveal-on-scroll, no parallax (brand anti-features). The only transition used in Phase 3 is `transform: translateX()` on the `→` arrow in the two links above, using `transition: transform .15s`.

---

## Performance / Image Contract

- **No client JS.** All Phase 3 pages declare `export const prerender = true`. `robots.txt.ts` is the only SSR endpoint (no Worker code, just env branching; acceptable per D-29).
- **About photo strip images:** use existing gallery `hero-800.webp` variants. No new prebuild step. `passthroughImageService()` remains (Sharp not supported in `workerd`). Include `width`, `height`, `loading="lazy"`, `decoding="async"` on strip photos (none is LCP on these pages; all below the fold). Use `object-fit: cover` on the 1:1 cells.
- **No new product photos are required for Phase 3 to ship** (D-14). Executor picks 3 existing gallery pieces by color balance during execution.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| Third-party registries | none | not applicable |

Rationale: same as Phase 2 — Tailwind + shadcn explicitly rejected by CLAUDE.md. `colors_and_type.css` + design-skill JSX components are the complete design system.

---

## Pre-Populated From

| Source | Decisions / values used |
|--------|-------------------------|
| `CLAUDE.md` | No-Tailwind / no-shadcn; vanilla CSS + Astro scoped styles; `passthroughImageService()`; font families; SSR-only React; `output: 'server'` kept (D-22) |
| `studio-bluemli-design/SKILL.md` + `colors_and_type.css` | All color tokens, all spacing tokens, all type tokens, all radii, brand non-negotiables (cream not white, no flower vocab, no gradients, no 1px borders, no backdrop-filter, sentence case, no emoji, ♡/♥ only, "pick 3 accents"), phase-3 three-accent palette (coral + indigo + olive) |
| `sketch-findings-bluemli/SKILL.md` + `references/pop-ups-across-surfaces.md` + `references/static-narrative-pages.md` | All Phase 3 layout dimensions (640px mini-callout, 720px popups/about/say-hi column); CSS class names; type sizes per role; eyebrow formats; interaction states; what-to-avoid list; HTML structures; empty-state copy; about photo strip grid pattern; /say-hi clamp headline |
| `03-CONTEXT.md` (D-01..D-29) | All 29 implementation decisions: Hero unchanged (D-01), mini-callout not PopupStrip (D-02), section omitted when zero upcoming (D-03), 3 featured pieces (D-04), no embedded CTA (D-05), one-prominent + also-coming-up + past (D-06), no past photos (D-07), empty-state copy (D-08), PopupStrip CTA deleted (D-09), no per-popup routes (D-10), about copy Claude-drafted (D-13), about reuses gallery WebPs (D-14), copy-first + photo strip below (D-15), "made with love from NOPA ♡" signature (D-16), no press section (D-17), no contact form (D-18), wordmark font swap (D-24), NOPA casing fix (D-25), SEO.astro props contract (D-26), logo-lockup default og:image (D-27), all 5 routes + gallery slugs in sitemap (D-28), robots.txt Astro endpoint (D-29) |
| `REQUIREMENTS.md` (PAG-01..PAG-09, FND-12, FND-13) | All 9 page-composition requirements; focus-visible + skip-to-content (FND-13); mobile-first Lighthouse ≥ 90 (FND-12) |
| Phase 2 `02-UI-SPEC.md` | Token table carry-forward; status-color contract; brand CI grep rules; focus ring pattern; performance/image contract |
| User input this session | none — every dimension is pre-answered by upstream artifacts |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
